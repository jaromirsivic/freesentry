Camera 1: Panel()
	Camera Source: ComboBox()
	Flip, Rotate and Crop: HorizontalSeparator()
		Flip Horizontally: Switch(value=no)
		Flip Vertically: Switch(value=no)
		Rotate: ComboBos()
		Crop Left (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
		Crop Top (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
		Crop Right (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
		Crop Bottom (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
	Resize: HorizontalSeparator()
		Enabled: switch(value=false)
		Width (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
		Height (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
	Sight Zero: HorizontalSeparator()
		X (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
		Y (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
	Attention Zones: HorizontalSeparator()
		X (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
		Y (px): NumericInput(minValue=0, decimalPlaces=0, step=1)

Create a new component called “Polygon”. 


Fix (or completely rewrite) the polygon component.
The user will be able to draw a closed polygon using this component. Whenever a user clicks he adds a new point. If a user is in the adding mode and he clicks on the very first point,
the polygon is closed, there will be no more points added to the polygon from this point. User can only move existing points or delete entire polygon and start drawing new one from scratch.
The component is based on the "Image" component with the same set of props (so it can display image behind the polygon) plus following props:
- borderColor: color – color of the polygon line, default value #009900ff
- fillColor: color – color of the inside of the polygon, default value #00ee0055
- lineWidth: number – width of the line in pixels, default value 1px
- maxPoints: identifies how many points can the polygon have, default value is 32.
- polygons: gets or sets an array of polygons, each polygon is identified by array of points. Entire structure is in json format.

Demonstrate the usage of the component in Sandbox -> ComponentDemo page

Update the behavior of the "Polygon". Whenever a polygon is closed by clicking on the first point or by
exhausting all points available the polygon should not disapear (current behavior). It must stay where it is
and be infilled with the fillColor. The size of the initial point will be the same as the size of all other
points (currently during drawing mode the size of the very first point is bigger than those of other points
that is OK, but after the polygon is closed the size should be set to the size of other point).
Update Sandbox -> ComponentDemo add a button under each polygon. After clicking the button
the alert message will pop up displaying polygons json. That way I will be able to check if the json is OK.

Update Polygon component. Add following props:
reticleX - x position of the reticle (value between 0 and 1)
reticleY - y position of the reticle (value between 0 and 1)
showReticle - boolean, default value is false
reticleSize - default value is 1
reticleAlpha - default value is 50% or rather 0.5 which means that the reticle is half transparent
reticleColor - color of the reticle, default value is red.
Implement reticle which is displayed if showReticle is true and uses theme like in the picture enclosed to this chat.
Add new examples to the ComponentsDemo page showing various capabilities of reticle.

# ColorPicker
Create a new ColorPicker component. The user will be able to select a color from the color picker, or
specify the color using the hex code. The component should have the following props:
- color: string – the selected color
- onChange: function – the function to be called when the color is changed
- showHex: boolean – whether to show the hex code input or not
- showAlpha: boolean – whether to show the alpha input or not
Demonstrate the usage of the component in Sandbox -> ComponentDemo page

Update the Polygon component. 
- Remove "reticleAlpha" prop, the alpha channel will be included in "reticleColor".
- add "mode" props. "mode" can be one of: "viewer", "designer", "joystick".
In a viewer mode the user can only see image, polygons
and reticle (of course polygons and reticle will be visible if there are any
polygons and if the showReticle is true). In th "designer" mode the user can draw
polygons. Joystick mode will be implemented later. Do not bother with joystick mode at the moment.

Update the Polygon component.
Implement the "joystick" mode.
In the "joystick" mode user cannot draw or edit polygons.
In the "joystick" mode the user can click on any part of the component.
As soon as he clicks on the image the initial point called static point is displayed as a circle
with color "joystickColor" (default value is #999999cc) and size "joystickSize".
On top of it is another circle called dynamic point again with color "joystickColor" and size "joystickSize".
The centers of the circle are connected with a dotted line of width "joystickLineWidth" (default value is 2).
There will be a "joystickLineMaxLength" prop which will define the maximum length of the line
and therefore maximum distance between the centers of two circles. The default value of "joystickLineMaxLength"
is 0.25 which means that the maximum distance between the centers of two circles is 25% of the image size
computed from the smaller dimension of the image. If the user clicks on the image, moves the mouse or fingertip
while still pressing left mouse button or fingertip - and the distance between the centers of the two circles
is greater than the maximum length, the dynamic point will be placed at the point on the image that is at the
maximum length from the static point and at the same time closest to the clicked point or fingertip.
If the user releases the left mouse button or fingertip the dynamic point will snap to the clicked point
and disappear. The animation length is defined by "joystickSnapAnimationDuration" prop (default value is 0.1s).
Demonstrate the usage of the component in Sandbox -> ComponentDemo page.

Update the Polygon component.
Add event "onJoystickMove". It will be called when the dynamic point moves.
There are x, y parameters of the event object.
The x, y parameters are the coordinates of the dynamic point relative to the static point.
Min value is -1, max value is 1.
Add "joystickLineColor1" prop. It defines the color of the line when the dynamic
point is on top of the static point. Default value is #5555ffff.
Add "joystickLineColor2" prop. It defines the color of the line when the dynamic
point is as far as possible from the static point. Default value is #ff0000ff.

Demonstrate the usage of the component in Sandbox -> ComponentDemo page.
Display x, y coordinates of the dynamic point relative to the static point in the label.


Update the Polygon component.
Add "joystickZeroRadius" prop. It defines the radius of the circle when the
dynamic point is almost on top of the static point. In this case the onJoystickMove event is still
called but the x, y parameters are 0.
Default value is 0.03.

There is a problem with the Slider component on a mobile devices.
I have tried to use the Slider component from the chrome web browser on the mobile phone and it does
not allow me to move slider. I can only click on it. Apparently when i press and hold finger in the browser
(above the slider), the browser thinks I want to scroll the
page and it blocks my attempts to move the slider. Fix the issue.


There is a problem with the Polygon component on a mobile devices.
I have tried to use the Polygon component from the chrome web browser on the mobile phone and it does
not allow me to move move points of polygon. I can only click on it to add a new point.
Apparently when i press and hold finger in the browser
(above the polygon), the browser thinks I want to scroll the
page and it blocks my attempts to move the points of the polygon.
The same applies in the joystick mode.
Fix the issue.

There is still one issue with Polygon component on a mobile devices.
When I am in "joystick" mode and I click and hold the finger on the image
and I do not move it the browser shows me a popup asking me if I want to
download the image. This should not happen. Fix the issue.

There is a problem with the Scene3D component on a mobile devices.
I have tried to use the Scene3D component from the chrome web browser on the mobile phone and it does
not allow me to move the camera. Apparently when I press and hold finger in the browser
(above the Scene3D), the browser thinks I want to scroll the
page and it blocks my attempts to move, rotate or zoom the camera.
Fix the issue.

There is a problem on all pages when they are displayed on a mobile phone.
The top part of the content area is cut off (it is hidden behind Menu Bar).
The bottom part of the content area is cut off (it is hidden behind mobile phone action buttons).
Fix the issue.

Update Settings -> Camera page. The first panel should look like this: 
Camera: Panel()
	Camera Source: Text()
	Flip, Rotate and Crop: HorizontalSeparator()
		Flip Horizontally: Text()
		Flip Vertically: Text()
		Rotate: Text()
		Crop Left (px): Text()
		Crop Top (px): Text()
		Crop Right (px): Text()
		Crop Bottom (px): Text()
	Resize: HorizontalSeparator()
		Enabled: Text()
		Width (px): Text()
		Height (px): Text()
	Reticle: HorizontalSeparator()
		Show Reticle: Text()
		X (%): Text()
		Y (%): Text()
		Color: ColorPicker(enabled=false)
		Size: Text()

Make all labels of Text component and ColorPicker component bold.
There is an edit button in the header of the panel which displays
a modal window with the following components:

Camera: Panel()
	Camera Source: ComboBox()
	Flip, Rotate and Crop: HorizontalSeparator()
		Flip Horizontally: Switch(value=no)
		Flip Vertically: Switch(value=no)
		Rotate: ComboBox(values=["0", "90", "180", "270"])
		Crop Left (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
		Crop Top (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
		Crop Right (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
		Crop Bottom (px): NumericInput(minValue=0, decimalPlaces=0, step=1)
	Resize: HorizontalSeparator()
		Enabled: switch(value=true)
		Width (px): NumericInput(value=640, minValue=0, decimalPlaces=0, step=1)
		Height (px): NumericInput(value=480, minValue=0, decimalPlaces=0, step=1)
	Reticle: HorizontalSeparator()
		Show Reticle: Switch(value=true)
		X (%): NumericInput(value=50, minValue=0, maxValue=100, decimalPlaces=3, step=0.5)
		Y (%): NumericInput(value=50, minValue=0, maxValue=100, decimalPlaces=3, step=0.5)
		Color: ColorPicker()
		Size: NumericInput(minValue=0.25, maxValue=5, decimalPlaces=2, step=0.25)

There is no interaction with the backend at the moment.

Update Settings -> Camera page
First Panel.
Do not make labels bold. Make bold the values after the ":".
On the model window after the edit button is clicked make the vertical spacing
between the components 0.5rem.
Update modal window using following setup:

Camera: ModalWindow()
	Camera Source: ComboBox()
	Flip, Rotate and Crop: HorizontalSeparator()
		Flip Horizontally: Switch(value=no)
		Flip Vertically: Switch(value=no)
		Rotate: ComboBox(values=["0", "90", "180", "270"])
		Crop Left (%): Slider(minValue=0, maxValue=100, decimalPlaces=2, step=0.5)
		Crop Top (%): Slider(minValue=0, maxValue=100, decimalPlaces=2, step=0.5)
		Crop Right (%): Slider(minValue=0, maxValue=100, decimalPlaces=2, step=0.5)
		Crop Bottom (%): Slider(minValue=0, maxValue=100, decimalPlaces=2, step=0.5)
	Resize: HorizontalSeparator()
		Enabled: switch(value=true)
		Width (px): Slider(value=640, minValue=0, decimalPlaces=0, step=1)
		Height (px): Slider(value=480, minValue=0, decimalPlaces=0, step=1)
	Reticle: HorizontalSeparator()
		Show Reticle: Switch(value=true)
		X (%): Slider(value=50, minValue=0, maxValue=100, decimalPlaces=2, step=0.5)
		Y (%): Slider(value=50, minValue=0, maxValue=100, decimalPlaces=2, step=0.5)
		Color: ColorPicker()
		Size: Slider(minValue=0.25, maxValue=5, decimalPlaces=2, step=0.25)
Rename OK button to "Save".

Update Camera -> Settings -> Modal Window:
1. In section "Resize" the "Width (px)" Slider should have a min value 320
2. In section "Resize" the "Height (px)" Slider should have a min value 240
3. In section Flip, Rotate and Crop. All crop sliders should have step 0.25. (two decimal places should be always visible in the NumericInput on the right side of the Slider)
4. In Reticle section the X, Y sliders should have a step 0.1. (two decimal places should be always visible)
5. In Reticle section the Size slider should have a step 0.1. (two decimal places should be always visible)
6. Add a business validation: (Crop Left + Crop Right) < 100
7. Add a business validation (Crop Top + Crop Botton) < 100

Create a Settings -> ImportExport page.
There will be following structure of the gui:

ImportExport: Panel()
	You can import settings from a file (that will override the current settings) or export settings to a file or restore settings to factory defaults.
	Import: HorizontalSeparator()
		Import from file: FileInput()
	Export: HorizontalSeparator()
		Export to file: Button()
	Factory Defaults: HorizontalSeparator()
		Restore Factory Settings: Button()

If user clicks on "Import" button, the system should ask for confirmation, informing user that current settings of Motors, Camera, AI, ... will be overriden.
User needs to confirm that.
If user clicks on "Export" button the page will download settings.json.
If user clicks on "Restore Factory Settings" button, the system should ask for confirmation, informing user that current settings of Motors, Camera, AI, ... will be restored to factory defaults.
User needs to confirm that.

Create a FileInput component.

On a Settings -> Hot Zone page add an edit button to the Menu Bar on the left side of the menu button.
Use the same style as in the Motors page.
When user clicks on the edit button a modal window should open.

Hot Zone: ModalWindow()
	Units: MultiSwitch(values=["cm", "in"])
	Center Pole: HorizontalSeparator()
		Mic Stick Radius ({unit}): Slider(minValue=10, maxValue=300, decimalPlaces=1, step=0.1)
		Height ({unit}): Slider(minValue=0, maxValue=300, decimalPlaces=1, step=0.1)
		Y Distance from Arm Poles ({unit}): Slider(minValue=-100, maxValue=300, decimalPlaces=1, step=0.1)
	Arm Poles: HorizontalSeparator()
		Height ({unit}): Slider(minValue=0, maxValue=300, decimalPlaces=1, step=0.1)
		X Distance ({unit}): Slider(minValue=10, maxValue=300, decimalPlaces=1, step=0.1)
	Arms: HorizontalSeparator()
		Left Arm Min Length ({unit}): Slider(minValue=1, maxValue=300, decimalPlaces=1, step=0.1)
		Left Arm Max Length ({unit}): Slider(minValue=1, maxValue=300, decimalPlaces=1, step=0.1)
		Right Arm Min Length ({unit}): Slider(minValue=1, maxValue=300, decimalPlaces=1, step=0.1)
		Right Arm Max Length ({unit}): Slider(minValue=1, maxValue=300, decimalPlaces=1, step=0.1)

Business validations:
	Left Arm Min Length < Left Arm Max Length
	Right Arm Min Length < Right Arm Max Length

Add a section into the settings.json to store the hot zone settings.
Update backend rest api to store the hot zone settings.
Update backend rest api to get the hot zone settings.

Add following business validations to HotZoneEditModal:
1. Left Arm Min Length < Left Arm Max Length
2. Right Arm Min Length < Right Arm Max Length
Use the same style of warning button and warning logic as it is used on Motors Page.

Create a new file "HotZoneCloudPointsGenerator" with a logic to compute
x, y, z coordinates of cloud points of hot zone. The logic works like this:
1. Create a function to compute intersection of three spheres.
2. Compute a spheres center points:
2a. Left Sphere Center Point is calculated as follows: {
	X = - (armPoles.xDistance / 2)
	Y = 0
	Z = armPoles.height
}
2b. Right Sphere Center Point is calculated as follows: {
	X = (armPoles.xDistance / 2)
	Y = 0
	Z = armPoles.height
}
2c. Top Sphere Center Point is calculated as follows: {
	X = 0
	Y = centerPole.yDistanceFromArmPoles
	Z = centerPole.height
}

In a loop do this:
cloud_points =[]
for i1 in range(0, calculationQuality):
	for i2 in range(0, calculationQuality):
		sphereLeftRadius = arms.leftArmMinLength + (arms.leftArmMaxLength - arms.leftArmMinLength) * i1 / calculationQuality
		sphereRightRadius = arms.rightArmMinLength + (arms.rightArmMaxLength - arms.rightArmMinLength) * i2 / calculationQuality
		sphereCenterRadius = centerPole.micStickRadius
		cloud_points.append(computeIntersectionOfThreeSpheres(leftSphereCenterPoint, rightSphereCenterPoint, centerSphereCenterPoint, sphereLeftRadius, sphereRightRadius, sphereCenterRadius))

When a save button is clicked, the cloud points should be displayed in the Scene3D using blue color.
Previous Scene3D should be cleared.

There should be as well displayed:
- leftPole as a black line of width 4 from:
	(leftSphereCenterPoint.x, leftSphereCenterPoint.y, 0) to (leftSphereCenterPoint.x, leftSphereCenterPoint.y, leftSphereCenterPoint.z)
- rightPole as a black line of width 4 from:
	(rightSphereCenterPoint.x, rightSphereCenterPoint.y, 0) to (rightSphereCenterPoint.x, rightSphereCenterPoint.y, rightSphereCenterPoint.z)
- centerPole as a black line of width 4 from:
	(centerSphereCenterPoint.x, centerSphereCenterPoint.y, 0) to (centerSphereCenterPoint.x, centerSphereCenterPoint.y, centerSphereCenterPoint.z)

On top of each pole there should be displayed an anchor sphere.
Color of left anchor sphere should be red.
Color of right anchor sphere should be green.
Color of center anchor sphere should be blue.

Update HotZoneCloudPointsGenerator.js in a way that it computes the four boundary points.
Boundary points are computed as follows:
Top Boundary Point is computed as intersection of Left Sphere with radius leftArmMinLength and Right Sphere with radius rightArmMinLength and center sphere with radius centerPole.micStickRadius.
Left Boundary Point is computed as intersection of Left Sphere with radius leftArmMaxLength and Right Sphere with radius rightArmMinLength and center sphere with radius centerPole.micStickRadius.
Bottom Boundary Point is computed as intersection of Left Sphere with radius leftArmMinLength and Right Sphere with radius rightArmMaxLength and center sphere with radius centerPole.micStickRadius.
Right Boundary Point is computed as intersection of Left Sphere with radius leftArmMaxLength and Right Sphere with radius rightArmMaxLength and center sphere with radius centerPole.micStickRadius.
Since the Intersection of three spheres can produce multiple points, we need to select the point that is closest to the ground (z coordinate is the smallest).

Update Hot Zone page to display the BoundaryStructure. BoundaryStructure is rendered as a set of lines:
1. Red line of width 0.05 from (leftSphereCenterPoint.x, leftSphereCenterPoint.y, leftSphereCenterPoint.z) to (topBoundaryPoint.x, topBoundaryPoint.y, topBoundaryPoint.z)
2. Green line of width 0.05 from (rightSphereCenterPoint.x, rightSphereCenterPoint.y, rightSphereCenterPoint.z) to (topBoundaryPoint.x, topBoundaryPoint.y, topBoundaryPoint.z)
3. Blue line of width 0.05 from (centerSphereCenterPoint.x, centerSphereCenterPoint.y, centerSphereCenterPoint.z) to (topBoundaryPoint.x, topBoundaryPoint.y, topBoundaryPoint.z)
4. Blue line of width 0.05 from (centerSphereCenterPoint.x, centerSphereCenterPoint.y, centerSphereCenterPoint.z) to (leftBoundaryPoint.x, leftBoundaryPoint.y, leftBoundaryPoint.z)
5. Blue line of width 0.05 from (centerSphereCenterPoint.x, centerSphereCenterPoint.y, centerSphereCenterPoint.z) to (bottomBoundaryPoint.x, bottomBoundaryPoint.y, bottomBoundaryPoint.z)
6. Blue line of width 0.05 from (centerSphereCenterPoint.x, centerSphereCenterPoint.y, centerSphereCenterPoint.z) to (rightBoundaryPoint.x, rightBoundaryPoint.y, rightBoundaryPoint.z)

1. When drawing points use red color for those points which have z coordinate smaller than the ground level (z=0)
2. When drawing points use #bbbb00 color for those points which are above the plane defined by topBoundaryPoint, centerSphereCenterPoint, rightBoundaryPoint
or which are above the plane defined by topBoundaryPoint, leftBoundaryPoint and centerSphereCenterPoint

Add a new business validation on the Hot Zone Edit Modal Window to check that all 4 boundary Points exist.
If any of the boundary points do not exist, show a warning message starting with text:
"Invalid Hot Zone Settings. With this setup the motors will not be able to move
from fully retracted position to fully extended position. Please adjust the hot zone settings."

Update Modal Window. Add prop "movable" to the modal window.
When movable is true, the modal window should be movable.
When movable is false, the modal window should not be movable.
Default value of movable is false.

1. Update Hot Zone Modal window. Whenever any Slider value is changed,
meaning mouse up event is triggered, the Scene3D should be updated
but the configuration should not be saved to the settings.json file.
Configuration will be saved only when the Save button is clicked.
2. Update Hot Zone Modal window. The business validation regarding
existence of all 4 boundary points should be treated as an Error.
When the Error is triggered, the Save button should be disabled.
When the Error is cleared, the Save button should be enabled.

Update the Hot Zone. Add two TextFields to the Scene3D to the top left corner.
The first TextField should display the "Horizontal FOV: " value in degrees with two decimal places.
The second TextField should display the "Vertical FOV: " value in degrees with two decimal places.
The horizontal FOV is computed as follows an angle between leftBoundaryPoint, centerSphereCenterPoint and rightBoundaryPoint.
The vertical FOV is computed as follows an angle between topBoundaryPoint, centerSphereCenterPoint and bottomBoundaryPoint.
Create a TextField component if necessary. The TextField component should be used to display text.
It is not an input field, it is just a display field. There should be at least following props:
text: string
fontSize: number
fontColor: string
fontFamily: string
fontStyle: string
fontWeight: string

Update the Scene3D component Reset button which is used to reset the camera position to the default position.
When the Reset button is clicked, the camera position should be reset to the default position.
Default position of camera is (x=200, y=200, z=200). Camera is looking at point (x=0, y=0, z=0).

Create a Table component which should behave like an excel table and will have the following props:
- width: number - optional, by default the width is 100% of parent container
- height: number
- canAddRows: boolean - default false
- canAddColumns: boolean - default false
- canDeleteRows: boolean - default false
- canDeleteColumns: boolean - default false
- maxColumns: number - by default null meaning infinite number of columns. maxColumns is usable only in case that user canAddColumns.
- maxRows: number - by default null meaning infinite number of rows. maxRows is usable only in case that user canAddRows.
- cellsEditable: boolean - by default true
- columnsHeaders: array of objects with properties: name, width, align, backgroundColor, textColor, fontSize, fontFamily, fontStyle, fontWeight - identifying parameter of each column header cell. User cannot override text in header cells. Column header cells are pinned, they do not scroll if vertical scrollbar is used. Similar behavior like in Excel.
- rowsHeaders: array of objects with properties: name, height, align, backgroundColor, textColor, fontSize, fontFamily, fontStyle, fontWeight - identifying parameter of each row header cell. User cannot override text in header cells. Row header cells are pinned, they do not scroll if Horizontal scrollbar is used. Similar behavior like in Excel.
- cells: array of objects with properties: value, align, backgroundColor, textColor, fontSize, fontFamily, fontStyle, fontWeight, editable

RowsHeaders and columnsHeaders can be null.
Cells can be modified by the user. User can select multiple cells, write a number or text and use ctrl+enter to copy the value to all selected cells. Similar behavior like in Excel.
Copy, paste, cut, delete, undo, redo operations are supported.
User can drag the cell by the corner to copy its value - similar behavior as in Excel.
If enabled user can add new rows and columns.

- Cells area of the Table should be scrollable if necessary

Create a new web page in the sandbox and demonstrate the usage of the Table component

Update the table component. There are following issues:
1. When user selects multiple cells either by mouse or by clicking into the cell (lets say cell B1) then holding shift key and selecting another cell (lets say cell B2),
the cells beckground is highlighted correctly, but at the same time web browser highlights/selects as well the text in cells C1, D1, E1, ..., A2, because it probably thinks I want to select, copy and paste a plain text
and it does not recognize that those are different cells and that we are working in the excel like Table. You should correct this behavior so the text in the cells user did not select is not highlighted.
2. Selecting neighboring cells by holding Shift key + arrow key works only for nearest neighboring cell but not for neighbour of the neighbour.
When I press shift + right arrow it selects correctly the right cell from the currently selected cell, but if I press right arrow again nothing happens. The same applies to the top, left and bottom arrows.
3. Remove add buttons for adding new rows and new columns, which are displayed directly in the table. Use context (popup) menu instead.
In the context menu there should be at least following items:
- Copy
- Cut - enabled only if cells are editable, otherwise it is disabled
- Paste
HorizontalSeparator
- Add Column(s) - enabled only if canAddColumns is true. Will add column(s) after the latest selected cell. Typically it adds exactly one column,
but if multiple cells in multiple columns are selected (lets say cells A1, B1, C1, A2, B2, C2) it will add 3 columns after the column C (because cells in three distinct columns are selected and that dictates that 3 columns should be added).
- Add Rows(s) - enabled only if canAddRows is true. Will add row(s) after the latest selected cell. Typically it adds exactly one row,
but if multiple cells in multiple rows are selected (lets say cells A1, B1, C1, A2, B2, C2) it will add 2 rows after the row 2 (because cells in two distinct rows are selected).
- Delete Columns - enabled only if canDeleteColumns is true. Deletes columns of all currently selected cells.
- Delete Rows - enabled only if canDeleteRows is true. Deletes rows of all currently selected cells.



Update the table component. There are following issues:
1. Popup menu used to add rows and columns should be displayed above all other components (to not be cropped by parent panel).
If there is not enough space on the screen to display the menu below mouse cursor, display it above the cursor.
If there is not enough space on the screen to display the menu on the right of mouse cursor, display it on the left of the cursor.
2. Add following props to each column and implement logic for resizing column(s):
- minWidth
- maxWidth
- canResize - default value is false
3. Add following props to each row and implement logic for resizing row(s):
- minHeight
- maxHeight
- canResize - default value is false
4. The area on the bottom right corner of the cell (used when user wants to copy cell into multiple others like in Excel) is too small. Make it at least two times bigger.
5. I was not able to add multiple columns using context menu. The option on all examples is always disabled. May be it is not working.
6. I was not able to delete row or column. The option on all examples is always disabled. May be it is not working.
Fix the issues above and update the Table demo page in the sandbox. Add examples to add columns, rows and to resize columns and rows.


Update the table component. There are following issues:
1. There is a problem with the context menu. When I right click on the cell, the context menu is displayed, but when I click on any enabled option in the context menu, nothing happens.
- Copy, Cut, Paste options are not working.
- User cannot add new rows or columns. The options to add new columns or rows in the context menu are enabled, but when I click on them nothing happens.
- User cannot delete rows or columns. The options to delete columns or rows in the context menu are enabled, but when I click on them nothing happens.
2. The area on the bottom right corner of the cell (used when user wants to copy cell into multiple others like in Excel) is still too small. Make it at least two times bigger.
3. When user uses horizontal scrollbar to scroll the table, the header columns are not scrolled. The header columns should be scrolled as well.
4. Add onChange event to the table component. It should be triggered when user changes any cell value.
5. Add onSelectionChange event to the table component. It should be triggered when user changes selection of cells.
6. Add onAddColumn event to the table component. It should be triggered when user adds new column(s) using context menu.
7. Add onAddRow event to the table component. It should be triggered when user adds new row(s) using context menu.
8. Add onDeleteColumn event to the table component. It should be triggered when user deletes column(s) using context menu.
9. Add onDeleteRow event to the table component. It should be triggered when user deletes row(s) using context menu.
10. Add onResizeColumn event to the table component. It should be triggered when user resizes column(s) using context menu.
11. Add onResizeRow event to the table component. It should be triggered when user resizes row(s) using context menu.
12. Add onCopy event to the table component. It should be triggered when user copies cell(s) value(s) to other cell(s).
13. Add onCut event to the table component. It should be triggered when user cuts cell(s) value(s) to other cell(s).
14. Add onPaste event to the table component. It should be triggered when user pastes cell(s) value(s) to other cell(s).
Demonstrate changes in the sandbox.


NumericInput component when used on a modal window (for example on Settings -> Motors) is not in the same row
as the label. Fix it.

Update the menu component in a way that when the menu is displayed the content area has a gray overlay,
the same way as it is done when the modal window is displayed. When user clicks anywhere 
else except the menu, the menu should be closed.

Update Settings -> Motors page. Edit modal window.
Add new section under the duty cycle section. The section should be named "Speed histogram". There will be following components:
PWM Multiplier: Slider with range from 0 to 1. The value should be rounded to 2 decimal places.
MultiSwitch component. It should have 3 options: Revers, Stop, Forward. The default value should be Stop.
Histogram: Table component. It should have 3 columns: PWM Multiplier, Forward seconds, Reverse seconds.
User is not able to add columns to the table. User can add rows to the table. There should be at most 20 rows in the table.
User can delete rows from the talbe. There must remain at least 2 rows in the table.

Update motors modal window.
1. Inertia should be a slider with range from 0 to 2. The value should be rounded to 2 decimal places.
2. The section "Speed Histogram" should be renamed to "Histogram Quick Test".
3. In section "Histogram Quick Test" and above the Slider "PWM Multiplier" there should be a text:
    Use "PWM Multiplier and "Reverse" / "Forward" buttons to test how long it takes to extend the stroke of linear actuator
    from fully retracted position to fully extended position and vice versa.
4. Before the Table component "Histogram" there should be HorizontalSeparator named "Speed Histogram".

Update the Motors page and rest api to support loading/saving of the motors Histogram data.

1. On the Motors page in the modal window rename "Default State" multiSwitch to "Motor Action".
2. When user clicks in the motors modal window on the "Motor Action" component on "Reverse" or "Forward" button, the application
will call rest api with parameters: "pin_index", "pwm_multiplier". The application will wait for the response
from the rest api and it will reset internal timer called "Histogram Quick Test Timer". The timer should
be reset every time user clicks on the "Reverse" or "Forward" button. The timmer should be stopped when
user clicks on the "Stop" button or when the modal window is closed. 
The timer is updated every 100ms and its value is displayed below the
"Motor Action" MultiSwitch component.
3. On the backend side the action triggers creation and reset of J8 component. The J8[pin_index] should be set to pwm_multiplier.
4. The J8 is reset when user clicks on the "Stop" button or when the modal window is closed. 

Relocate/move the ./package/submoamoa folder and all its content to ./package/src/submoamoa. Be careful to all config files
and dependencies.

It seems that the timer on the modal window in the motors page does not work properly. The logic should be that when user
clicks on the "Reverse" or "Forward" button, the timer should be reset meaning the algorithm will save current datetime.
Then every 100ms the timer should be updated and the difference between the current datetime and the saved datetime should be displayed.
When user clicks on the "Stop" button or when the modal window is closed, the timer should be stopped.

Create a Chart2D component which has the following features:
1. It should have x and y axis.
2. It should have a label for x and y axis.
3. It should have a title.
4. It should have a background color (default white) and a grid (default color light gray) with predefined grid size.
5. It should be pannable if allowed in the props.
6. It should be zoomable if allowed in the props.
7. It should be scrollable if allowed in the props.
8. It should have datasets displayed on the chart.
9. Each dataset can have its own color.
10. Each dataset can have its own label.
11. Each dataset can have its own line width.
12. Each dataset can have its own line style.

Demonstrate usage of the Chart2D component in the sandbox.


1. When zooming the through the chart the grid should zoom as well. There should be a props
with zoom mode which switches the functionality to zoom only the x axis (y is not zoomed)
or the second option as it is implemented right now it will zoom x and y axis.
2. Points should not be visible. When user hovers the mouse near
the line the point should appear and line perpendicular to x and y axis should
appear as well going from the point to axes showing precise numbers of the point coordinates
on the x and y axis.

On the motors page on the panel add a new section named "Histogram" below the section "Duty Cycle". The new section
should have a Chart2D component displaying the speed histogram data.
X axis should have a label "Speed" (from 0 to 1) and Y axis should have a label "PWM Multiplier" (from 0 to 1).
The datasets should be named "Forward" and "Reverse". The datasets should have different colors. Forward will have
blue color and Reverse will have red color. The datasets should have line width of 2.
The datasets will be received via rest api. The rest api endpoint is /api/motors/speedhistogram.
The rest api will load histogram data from assets/settings.json -> motors -> histogram. It will create
a SpeedHistogram object (see speedhistogram.py). Use the loaded histogram from settings.json as a constructor
parameter "speed_histogram". Then use properties "forward_speed_histogram" and "reverse_speed_histogram"
as datasets for the Chart2D component. The datasets should be named "Forward" and "Reverse".

1. The histogram should take 100% width of the parent panel.
2. Currently the Histogram displays two datasets forward and reverse. Use two histograms instead. The first one will display data for forward dataset and the second will display data for reverse dataset. Hide the legend for each Histogram. Use title for the first histogram: "Forward Speed Histogram". Use title for the second histogram: "Reverse Speed Histogram".
3. Make the background of both histograms transparent not white.

Created new web page "General Setup" in the Settings and put it under the "Import / Export". There is one panel
the same style as in the Motors page. There are following text fields on the panel:
Section "Controller Setup"
1. Controller: MultiSwitch component (button1="Localhost Raspberry Pi", button2="Remote pigpio daemon").
By default it is selected "Localhost Raspberry Pi".
2. Remote host: TextField component. By default the address is 192.168.0.1.
3. Remote port: TextField component. By default the port is 8888.
There is an Edit button in the header of the panel (similar like it is on the Motors Page). When user clicks on the Edit button 
the modal window should open. The modal window should have the same style as in the Motors page.
The modal window should have the following components:
1. Controller: MultiSwitch component (button1="Localhost Raspberry Pi", button2="Remote pigpio daemon").
2. Remote host: TextBox component. By default the address is 192.168.0.1.
3. Remote port: NumericInput component(min=0, max=65535). By default the port is 8888.
4. Save button. When user clicks on the Save button the modal window should close. The setup should be saved to settings.json
and the panel should be updated.
If the user selects "Localhost Raspberry Pi" the remote host and remote port should be disabled.

If "Localhost Raspberry Pi" is selected then TextFields on the panel should be disabled and grayed out.
If "Remote pigpio daemon" is selected then TextFields on the panel should be enabled and not grayed out.

1. Update the get_j8 function in main.py to use the remote host and remote port from settings.json.
2. There should be as well a function to reset in the main.py. The reset function will reset j8 with
remote host and remote port from settings.json. The reset function should be called when user clicks
on the "Save" button in the modal window.
3. Take a look into the motors page edit modal window. At the moment the forward and reverse buttons
call backend api which resets the j8. This reset should as well be updated to include 
remote host and remote port from settings.json.
4. If the property "initialized" of the j8 is False after the reset then display a red TextField
on the panel "Controller Setup" with the label "Error: " and the "error_message" of the j8.
5. If the property "initialized" of the j8 is True after the reset then display a green TextField
on the panel "Controller Setup" with the label "Initialized: OK".

Add new business validations to the Motors edit button Modal Window.
There should be following validations for the Speed Histogram table:
1. Table should contain at least two rows.
2. Each cell in the table must be a number with at most 3 decimal places.
3. All values in the column "PWM Multiplier" must be between 0.0 and 1.0.
4. The first row (column "PWM Multiplier") should be 0.0.
5. The last row (column "PWM Multiplier") should be 1.0.
6. All values in the column "Forward sec." and "Reverse sec." must be between 0 and 10000.
7. Each value in the column "PWM Multiplier" at each row except the first one must be greater than the value at the previous row.
8. Each value in the column "Forward sec." at each row except the first one must be lower than the value at the previous row.
9. Each value in the column "Reverse sec." at each row except the first one must be lower than the value at the previous row.
If one or more of the business validations above are not met then user will not be able to save the settings and display
a red Error button. When user clicks on the Error button the red modal window with the error
message should open (same behavior like already implemented with other business validations).
Other already implemented business validations should work the same way and should nicely integrate
with the new business validations.

On the Motors page into the Motors Panel, below the "Inertia" textfield add a new button with text Test Motor.
When user clicks on the Test Motor button the modal window should open.
The modal window should have the same style as in the Motors page.
The modal window should have the following components:
Polygon component with joystick behavior joystickLineMaxLength should be 0.4.
User cannot add any vertices. The reticle is not displayed.
background of the polygon component is joystick_test.svg.
Polygon component will be a square. Width is the same as Modal Window width. Height is the same as width.
Polygon component should be centered in the modal window.

Update Motors page -> edit Modal Window. Change Type and Role components from Combobox to MultiSwitch.

Create a motorscontroller.py file with MotorsController class and necessary methods and functions.
The MotorsController class should
contain a j8 property of type J8, an array of motors created based on settings.json and an execute method
which will be executed
as a separate thread. Inside the execute method there should be an infinite loop which will
call go method of each enabled motor in the array. There will be as well a stop method which will be called
when the MotorsController is stopped or when the FastAPI server is stopped. This method will end the infinite loop
and delete all motors from the array and release the j8. There will be as well a reset method which
will delete all motors from the array, reset the j8 and create new motors based on settings.json.

Update the Settings -> Motors page. In the "Test Motor" modal window If the user uses joystick
and its position changes then send joystick y coordinates to the backend api. The backend api
should call move method of the Motor with the proper motor index and pass
the joystick y coordinates as the speed parameter. Since the coordinates change happens
very often you may keep the connection to the backend api open and send the coordinates
periodically until the joystick is released.

At the moment, when users web browser contacts the sytem it uses port 80 (http).
First it contacts NodeJS server which either responds to the request directly
or in case of rest API requests it forwards the request to the FastAPI server
using port 8000. Why are there two web server instances running on different ports.
FastAPI server according to configuration (line 2: import react from '@vitejs/plugin-react')
should support React. Why is it not used? Everything should run under one web server instance.

1. Update Motors page, "Test Motor" button, Modal Window. Do not use web sockets. Use Rest API
calls when joystick moves. Update as well backend.
2. There should be no text on the modal window. Currently there is:
Use the joystick below to control motor speed directly. Up/Down controls speed (Full extension / Full retraction).
Remove this text.
3. The Polygon component should have same width and height. It should be square. It should cover
the content area of the modal window.

1. Update Motors page, "Test Motor" button, Modal Window. Whenever a joystick is moved
the frontend should call the backend passing motorName not motorIndex. Update the front end and the backend.

Update the Polygon component:
1. Add new props "joystickMoveInterval" where user can specify the interval in miliseconds which
limits how often is the onJoystickMove called. For example if the "joystickMoveInterval" is 500 then
there can be at most two onJoystickMove calls. The "joystickMoveInterval" is non blocking which means
that rendering of joystick is not affected and in case the joysctick moved slightly during the interval
period another onJoystickMove will be triggered. On the other hand if joystick position did not change during
last interval the onJoystickMove will not be triggered. Default value of "joystickMoveInterval" is 0.
2. Add new props "joystickLineMaxLengthMultiplierMode" which defines how the real joystickLineMaxLength is computed.
At the moment the real joystickLineMaxLength is computed out of joystickLineMaxLength props and min(width, height)
of the Polygon component. This mode will be called "minWidthHeight" and it will be a default mode
of the Polygon component. There will be as well mode "width" and mode "height". Mode "width" will take
width of the polygon component into the computation of the real joystickLineMaxLength. Mode "height"
will tak height of the polygon component into the computation of the real joystickLineMaxLength.

Update the Polygon component. There is an issue with onJoystickMove in combination
with "joystickMoveInterval" in case that user is rapidly moving the joystick,
he releases it (so the movement stopped) in between two intervals. Aparently onJoystickEnd is triggered by
the Polygon component but then after the time interval elapses it triggers onJoystickMove event.
Correct behavior is this:
	//user clicks in the Polygon component
	onJoystickStart()
	onJoystickMove(x, y) - triggered when user moves the joystick
	//user releases the joystick
	onJoystickMove(0, 0) - if there was at least one movement after the onJoystickStart
	onJoystickEnd()
	//another onJoystickMove must not be sent here it can be sent only after
	//the user clicks again and it will contain proper coordinates

Update the Polygon component. There is an issue with onJoystickMove after onJoystickEnd was called.
When user mouse up happens (or on the mobile phone he is no longer pressing the screen). There should
be one last onJoystickMove event triggered with x=0, y=0, then onJoystickEnd should be triggered
and then no more onJoystickMoves should be called (unless user starts using joystick again by pressing
mouse button). Animation may still happen even after onJoystickEnd, but that will not trigger onJoystickMove.
Correct behavior of how events are triggered is this:
	//user clicks in the Polygon component
	onJoystickStart()
	//user moves mouse cursor or finger on the screen
	onJoystickMove(x, y)
	//user releases the joystick
	onJoystickMove(0, 0) - if there was at least one movement after the onJoystickStart
	onJoystickEnd()
	//another onJoystickMove must not be sent here even if the snap animation is being rendered.
	//it can be sent only after the user clicks again and starts moving the mouse

Update Settings -> Camera page:
We will start the design of this page from the scratch.
There will be three panels on the page.
The first panel is called "Camera". There will be following components on the panel:
- General Setup: HorizontalSeparator
- - Input Device Index: TextField(value=0)
- - Preferred resolution: TextField(value=1920 x 1080)
- - Accepted Resolution: TextField(1920 x 1080)
- Flip and Rotate: HorizontalSeparator
- - Flip Horizontally: Switch
- - Flip Vertically: Switch
- - Rotate (degrees): Combobox(possible values=[0, 90, 180, 270], value=0)
- Preview: HorizontalSeparator
- - Live Preview: Polygon(image=blank)
- - Show Full Screen: Button
The second panel is called "Manual Control - Input Img". There will be following components on the panel:
- Crop: HorizontalSeparator
- - Top: TextField(value="0% (0 pixels)")
- - Left: TextField(value="0% (0 pixels)")
- - Bottom: TextField(value="0% (0 pixels)")
- - Right: TextField(value="0% (0 pixels)")
- Stretch: HorizontalSeparator
- - Width: TextField(value="640 pixels")
- - Height: TextField(value="480 pixels")
- Preview: HorizontalSeparator
- - Live Preview: Polygon(image=blank)
- - Show Full Screen: Button
The second panel is called "AI Agent - Input Img". There will be following components on the panel:
- Crop: HorizontalSeparator
- - Top: TextField(value="0% (0 pixels)")
- - Left: TextField(value="0% (0 pixels)")
- - Bottom: TextField(value="0% (0 pixels)")
- - Right: TextField(value="0% (0 pixels)")
- Stretch: HorizontalSeparator
- - Width: TextField(value="640 pixels")
- - Height: TextField(value="480 pixels")
- Attention Area: HorizontalSeparator
- - Attention Area Enabled: Switch
- - Attention Area Polygon: Polygon(image=blank)
- - Show Full Screen: Button

Update camera page.
1. For each panel in the page create an Edit button in the header of the panel.
The same approach as on the Motors page. When user clicks on the Edit button
a Modal Window will appear. There will be appropriate input controls in the window.
2. On the Camera page in all panels change the TextBox components, Switch components
and ComboBox components to plain TextFields (the Same approach and style like on the Motors page)
3. Fix the following issue. When user tries to navigate to another page from the Camera page
nothing happens. He still remains on the Camera page. Fix it.

There are following issues on the Camera Page:
1. When I click on the Menu Button in the Menu and then when I click on the Main Page
the Main Page is not displayed, the url in the browser changes but I still see Camera page,
there must be something wrong with the single page application that it does not render
main page correctly and stays on the Camera Page.
Expected behavior: the Main Page is displayed.

Create a new web page Cameras.jsx. It will be placed in the menu just below the "Camera" row
and its label will be "Camera(s)". The page will look almost like the "Camera" page, except
there will be no "Preview" section in the first and second panel and no "Attention Area" in the third panel.
Those sections should be removed as well from the Modal Windows.
Make sure that user can correctly navigate to this page and from this page through the menu.
Update all necessary files to add this new page.

Update Cameras page -> Camera Panel -> edit button -> Modal Window:
1. There should be no "Accepted Resolution" displayed in the Modal Window.
2. Input Device and Preferred Resolution should be ComboBoxes
3. There is a horizontal scrollbar displayed in the modal window.
Probably the width of some components is too big. Make them smaller so the horizontal scroll bar will
disapear.

For each property mentioned in camerascontroller -> Camera._load_properties
create a property in the Camera class. Getter will look like this:
@property
def brightness(self) -> int:
    return self.camera.get(cv2.CAP_PROP_BRIGHTNESS)

Setter will look like this:
@brightness.setter
def brightness(self, value: int):
    self.camera.set(cv2.CAP_PROP_BRIGHTNESS, value)

Remove web page Camera.jsx. Delete the file, remove the route, delete backend api if there is any.

Update the "Slider" component. At the moment slider has "min" and "max" props which define the range of the slider
and as well range of the "NumericInput" component. There will be created "minSlider" and "maxSlider" props
which will define the range of the slider. Those will be min and max boundaries of the slider.
But the "NumericInput" component will still have "min" and "max" props as a boundaries for the value.
If "minSlider" or "maxSlider" are not defined, then "min" and "max" props will be used as boundaries for the slider.

Update the Camera web page. Add all Camera properties to the Camera -> Edit -> Modal Window. Do not add
those properties to the Camera -> Panel. The Camera -> Panel should only display the properties that are
visible to the user at the moment. The Modal Window should display all properties.
Now do not create any backend logic for the Camera properties. Just update the frontend.

Update Camera web page. All sliders should have allowManualInput prop set to true. All
sliders (the sliding part without caption and NumericInput) 
in the section "Image Settings" should be optically aligned and should have the same width.
There must be no horizontal scrollbar displayed in the modal window. Probably the width of some components is too big.
Make them smaller so the horizontal scroll bar will disapear.

Update all basic components which have label prop. Add labelWidth prop to all of them so we can control
the width of the label to align components.

Update Camera web page. All sliders in the "Controls" section should have allowManualInput prop set to true.
All components should be optically aligned. Optical alignment is done per section - in each section
the components (sliders, switches, comboboxes, ...) should be aligned to the same left position
(I am not talking about the labels, but about the input part of the components) 
and the same kind of components (again not labels, but the input part of the components) should have the same width.

Add gap: '1rem' to other components (if it is relevant). We should have the same gap between components.
We should always maintain the same strategy and same look and feel for the components.

1. Update the Camera web page. Values in the Camera Panel and on the Camera -> Edit -> Modal Window should
be loaded from the backend. Backend should return all properties of the Camera class stored in the settings.json
camera -> general section.
Some properties like "Input Device Index" and "Preferred Resolution" are comboboxes which have
list of possible values. Backend should create a list of those values by reading all available input devices
(stored in main.py -> master_controller -> cameras_controller -> cameras -> name) and supported_resolutions
(stored in main.py -> master_controller -> cameras_controller -> cameras -> supported_resolutions) and dynamically
create a structure like this:
{
    "input_devices": [
        {
            "value": 0,
            "label": "Input Device 0"
			"supported_resolutions": [
				{
					"width": 640,
					"height": 480,
					"label": "640 x 480"
				},
				{
					"width": 1280,
					"height": 720,
					"label": "1280 x 720"
				}
			]
        },
        {
            "value": 1,
            "label": "Input Device 1"
			"supported_resolutions": [
				{
					"width": 640,
					"height": 480,
					"label": "640 x 480"
				},
				{
					"width": 1280,
					"height": 720,
					"label": "1280 x 720"
				}
			]
        }
    ]
}
and incorporate this structure into the message sent to the frontend.
Do not bother with saving the properties to the backend at the moment. Focus only on loading the properties from the backend.

Update the Camera web page. Rename "Preferred Resolution" to "Resolution". Rename "Input Device Index" to "Input Device".
Resolution should change when Input Device changes. List of resolutions should be loaded
from the "supported_resolutions" property which was sent by the backend to the frontend. If there is no
item in the list, then the ComboBox will have only one item "0 x 0".

Update camera web page. Add a new section "Preview" to the Camera Panel.
There should be following components in the section:
	Enabled: Switch
	Live Preview: Polygon
The Polygon component is stretched to fill the entire width of the section/panel.
The height of the Polygon component is computed based on the "4/3" aspect ratio.
If the enabled switch is off, then the "assets/camera.svg" should be displayed in the Polygon component
in the fit mode. The polygon should be optically aligned with the camera image.
Do not implement any backend logic for the Preview section at the moment.

The Polygon component added to the Cameras web page line 279-285 is causing a routing issues.
When the Polygon was added to the Cameras web page whenever user clicks on any other item in the menu
trying to navigate out of the page, nothing happens. Url in the web browser changes
but then nothing happens. The user is stuck on the Camera web page.
When the Polygon component is removed the routing starts to work correctly.
Find the issue and fix it. I need ot use Polygon component on this page.

The issue was not fixed by your changes. But I have found what can
remove the issue. If I add polygons={polygons1}
(see line 286) the routing issue dissapears. Why? Can you fix the root cause?

Update camera web page. Add a new section "Preview" 
to the "Manual Control - Input Img." and "AI Agent - Input Img." panels.
There should be following components in the section:
	Enabled: Switch
	Live Preview: Polygon
The Polygon component is stretched to fill the entire width of the section/panel.
The height of the Polygon component is computed based on the "4/3" aspect ratio.
If the enabled switch is off, then the "assets/camera.svg" should be displayed in the Polygon component
in the fit mode. The polygon should be optically aligned with the camera image.
Do not implement any backend logic for the Preview section at the moment.

Update Camera web page. When user enables preview in the camera panel the Polygon component
should display live video/image feed of the camera from the backend. Source of the feed is:
master_controller.cameras_controller.cameras[camera_index].image.frame
camera_index is saved in settings.json -> cameras -> general -> deviceIndex
Update Cameras.jsx, Polygon.jsx, main.py and other files if necessary.

Update Camera web page. When user enables preview in the "Manual Control - Input Img." panel the Polygon component
should display live video/image feed of the camera from the backend. Source of the feed is:
master_controller.cameras_controller.cameras[camera_index].image_cropped_resized.frame
camera_index is saved in settings.json -> cameras -> general -> deviceIndex

Update Camera web page. When user enables preview in the "AI Agent - Input Img." panel the Polygon component
should display live video/image feed of the camera from the backend. Source of the feed is:
master_controller.cameras_controller.cameras[camera_index].image_ai.frame
camera_index is saved in settings.json -> cameras -> general -> deviceIndex

On the Camera page on the Modal Window of the Camera Panel add following components:
Into the section general setup add:
FPS: Slider (minvalue=1, maxvalue=240)

On the Camera web page on the left side of the edit button in the header of the Camera panel add
a reload button with icon ./assets/reload.svg. When user clicks on 
the reload button the yes/no Modal Window with a question:
	Do you want to refresh list of camera devices? This operation may take several seconds.
If the user clicks on the "yes" button all displayed live streams in the Preview section
of all panels are terminated and "Enabled" switches in those Preview sections are turned off. The Backend api /reset is called
and the master_controller.cameras_controller.reset() function is called.

When user clicks on the Reload button on the Camera web page and then clicks
on the yes button on the Modal Window the button should change color and be disabled
and a text "Saving..." should appear. The same behavior like on the General Setup
web page should be used.

Split main.py into several files:
for example all rest api calls and all logic regarding motors should be placed into restapimotors.py
all rest api calls and all logic regarding camreas should be placed into restapicameras.py
and so on.
Each page will have its restapi{pagename}.py file

Update Camera web page -> Camera panel Modal Window and restapicameras.py -> /api/cameras/list.
Modify current behavior in the rest api. Currently we are returning back from the get_cameras_list_endpoint()
only the list of device names and for each device only a list of supported resolutions.
In the updated version we will return full list of all properties
of each camera using camera.to_dict() function. We will then load and display those properties
on the Modal Window - components for each property should already be there.

1. Update Camera web page. The get_cameras_list_endpoint() now almost returns correct json. The json looks like this:
{
    "controllerSetup": {
        "controller": "remote",
        "remoteHost": "192.168.68.56",
        "remotePort": 8888
    },
    "input_devices": [
        {
            "index": 0,
            "name": "0: GUID_1.0",
            "supported_resolutions": [
                {
                    "width": 320,
                    "height": 240,
                    "label": "320 x 240"
                },
                {
                    "width": 640,
                    "height": 480,
                    "label": "640 x 480"
                },
                {
                    "width": 1280,
                    "height": 720,
                    "label": "1280 x 720"
                },
                {
                    "width": 1920,
                    "height": 1080,
                    "label": "1920 x 1080"
                }
            ],
            "width": 1920,
            "height": 1080,
            "fps": 30.0,
            "bitrate": -1.0,
            "buffer_size": 1.0,
            "flip_horizontal": false,
            "flip_vertical": false,
            "rotate": 0,
            "brightness": 128.0,
            "contrast": 32.0,
            "hue": 0.0,
            "saturation": 64.0,
            "sharpness": 3.0,
            "gamma": 120.0,
            "white_balance_temperature": -1.0,
            "backlight_compensation": 1.0,
            "gain": -1.0,
            "focus": -1.0,
            "exposure": -6.0,
            "auto_white_balance_temperature": true,
            "auto_focus": true,
            "auto_exposure": false,
            "value": 0,
            "label": "0: GUID_1.0"
        },
		...
In the "input_devices" structure get rid of "value" and "label" those are redundant information.
If Cameras.jsx needs them then remap it to "index" and "name".

2. Make sure that the inputs in the Modal Window which are displayed after user clicks on the edit button in the Camera panel
are correctly set based on the last result of rest api call /api/cameras/list. For example brightness is correctly returned
as 128 in the json but the value of "Brightness" Slider in the section "Image Settings" in the Modal Window
is 0. There must be some error. Fix it.

Update Cameras web page I know where is the issue with the Modal Window. If the user selects
another input device and then goes back to the input device with index 0 the inputs in the Modal window
are set to correct values. It seems that the first display of the Modal Window does not refresh
all inputs and there are some default values but not the values based on the json received from backend.
Fix it.

1. Update Camera web page -> edit button -> Modal Window. Add "Apply" button between
Save and Cancel button. Apply button should have the same color as Save button.
1b. Remove "Accepted Resolution" TextField from the Camera panel and put "FPS" instead.
2. Create a backend function /api/cameras/savecamera to handle "save" and "apply" actions:
2a. When Apply button is pressed the setup of each camera (which could have been modified by
the user when interacting with properrties of the cameras in the modal window)
is sent back to the backend. Backend sets up "settings" property of all
cameras in the master_controller.cameras_controller.cameras[CAMERA_INDEX].settings and returns true to the front end.
Front end then immediately calls /api/cameras/list and refreshes all inputs on the modal window.
Nothing is saved to the settings.json. Modal window is not closed.
2b. When save button is pressed the behavior is almost the same as when the Apply button was pressed
except this time the setup of all cameras is saved into the settings.json
for each camera into the settings.json -> cameras[CAMERA_INDEX].general. then the Modal
window is closed. Rest api to list cameras is called but this time only to update the textfields of the Camera panel.

Add a Preview section into the Camera -> Edit -> Modal window as the last section of the Modal Window.
There will be a Polygon component in the section
connected to the camera live feed in the same way like it is done on the Camera panel
except here it is always enabled and immediately consumes a camera live feed. When the modal is closed
the preview on the Modal window stops consuming live feed.
The preview is not mentioned in the jsons exchanged with the backend. It exists only temporarily on the Modal Window.

The preview section with the Polygon on the Camera -> Edit -> Modal Window should 
display the live feed only in case the Enabled switch in the Preview section on the
Camera panel is turned on. Otherwise there should be displayed the same icon
as in the Camera panel and live feed is not consumed.

1. Add a preview section into the Manual Control - Input Img -> Modal Window. The behavior should be analogous
to the behavior of the Preview section in the Camera -> Modal Window.
2. Add a preview section into the AI Agent - Input Img -> Modal Window. The behavior should be analogous
to the behavior of the Preview section in the Camera -> Modal Window.

Update Camera web page -> edit button -> Modal window. When user changes "Input Device"
all live feeds in all polygons (on modal windows and as well in panels) should be stopped.
Then those live feeds which are enabled should be started again this time with different input device id.

Update Camera web page -> edit button -> Modal window. When user changes "Resolution"
all live feeds in all polygons (on modal windows and as well in panels) should be stopped.
Then those live feeds which are enabled should be started again.

Update reset button functionallity.
When user clicks on a reset button in the header of Camera panel and presses a yes button
for all cameras the master_controller.cameras_controller.cameras[CAMERA_INDEX].reset_settings() should
be called and cameras.general section of each camera should be deleted in settings.json

Update the reset button functionallity on the Cameras web page.
When the reset is finished call /api/cameras/list and update
values of input components on all panels and in Modal Windows.

Update the Polygon component. Add new porps "zoomPanEnabled". Default value is false.
If the zoomPanEnabled props is true
the user can use ctrl+"+" and ctrl+"-" to zoom in and zoom out and ctrl+0 to reset zoom.
User can also use mouse scroll wheel to zoom in and zoom out. In that case it zooms into the
position of the mouse cursor. User cannot zoom out (negative zoom is not allowed).
Zoom works in all stretch modes
always maintaining the correct aspect ratio (if the stretchMode is "fit" or "original"
the aspect ratio is the same as the aspect ratio of the original image. If
the stretchMode is "stretch" the aspect ratio is the ratio between width and height of
the Polygon component).
When implementing this feature make sure it works on mobile devices where
user uses two fingers to zoom in and zoom out.

To pan left and right user can use ctrl+"arrow left" and ctrl+"arrow right".
To pan up and down user can use ctrl+"arrow up" and ctrl+"arrow down".
User can also use mouse to pan left and right and up and down by pressing
right mouse button and moving mouse cursor left and right and up and down.
User cannot pan outside of the image meaning the image cannot be moved
outside of the visible area of the Polygon component.

Remove panning functionality from Polygon component. Remove as well all keyboard interactions.
Focus only on zooming functionality. Zoom can be done by mouse scroll wheel or two fingers on mobile devices.
Center of the zoom is always the mouse cursor position or the center of the two fingers on mobile devices.

1. Update Cameras web page -> Manual Control Input Img. Modal window. In the crop section
of the modal window the inputs should be Sliders. Label should contain information
about pixels and it should be computed dynamically: It should look like this:
Top % (0 pixels): Slider(minValue=0, maxValue=100, step=0.5, decimalPlaces=2)
Left % (0 pixels): Slider(minValue=0, maxValue=100, step=0.5, decimalPlaces=2)
Bottom % (0 pixels): Slider(minValue=0, maxValue=100, step=0.5, decimalPlaces=2)
Right % (0 pixels): Slider(minValue=0, maxValue=100, step=0.5, decimalPlaces=2)
Sliders should be optically aligned like in the Camera Modal window in
the Image Settings section.
Whenever user updates a value of the input either by dragging slider or by updating
value in the numeric input at the end of the slider the pixels information in the label
should be recalculated using following formula for top and bottom:
	pixels = slider.value * "Camera Panel"."General Setup"."Resolution"."height"
for left and right:
	pixels = slider.value * "Camera Panel"."General Setup"."Resolution"."width"
2. Update Cameras web page -> Manual Control Input Img. Modal window. In the Stretch
section the Width and Height should be NumericInputs. They should be optically alligned.
The width min value is 0, the width max value is 8192. Step is 1. There are 0 decimal places.
The height min value is 0, the height max value is 4608. Step is 1. There are 0 decimal places.

Update Cameras web page -> Manual Control Input Img. Modal window. In the crop section
of the modal window the numeric inputs should have a step 0.01.

Update Cameras web page -> Manual Control Input Img. Modal window. Add an Apply button
between Save and Cancel button in a similar way like it is on the Camera Modal Window
Do not implement any backend action at this moment.

Update Cameras web page -> Manual Control Input Img. Modal window. Add a business
validation. If the Crop left + Crop right >= 99% or Crop top + Crop bottom >= 99% then
show the red error button. When user clicks on it, display appropriate error
message (see examples in the Motors page how the errors and warnings on the Modal
windows should be developed). If there are errors on the Modal window, the Apply
and Save buttons are disabled.

Update Cameras web page -> AI Agent Input Img Modal window.
There should be the same features implemented in this modal window as they are in the
Manual Control input img Modal Window. Namely:
- Crop section - Sliders should be used. Dynamic recalculation to pixels should be
implemented
- Stretch section - The similar behavior like in Manual Control Modal window
- Apply button should be added
- Business validations should be added
- ...

Update Cameras web page. On the Manual Control - Input Img Panel and
AI Agent - Input Img Panel the values in the crop section
in percentages should always be displayed with 2 decimal places.

Update Cameras web page. Rename "preferred resolution" to just "resolution" and get rid of
"accepted resolution". Accepted resolution should not be used anywhere

When Saving camera setup by clicking on the Save button in the Camera Modal Window
save the "name" of the camera - send it to the backend API.
Camera Index and Name should be stored as well in the
"primaryCamera" section in the settings.json.

Update Cameras web page. When Camera web page is loaded the page should load
as well the primaryCamera and automatically select the input device
which is equal to the primaryCamera.index (you should add a new rest
api endpoint "getPrimaryCamera")

When user opens the camera modal window you should remember the settings used when the modal window was opened because if the user updates some properties or changes the input device and then at the end he clicks on the “Cancel” button you should revert back to the original settings used when the modal window was opened to undo all user updates.
When the user changes anything on the camera modal window and then if he clicks on the “Cancel” button you should ask him a question shown in the orrange modal window: Modifications you have made will be lost. Do you want to continue? If he clicks on “Yes” then you should reapply the original settings and close the modal window.

1. Update Camera web page and the backend application rest api to allow user to apply changes
of the Manual Control - Input Img Modal window to the
master_controller.cameras_controller.cameras[CAMERA_INDEX].image_cropped_resized when
the user presses Apply button. You will probably need to extend "list" api.
2. Implement as well the functionallity for the Save button.
Take a look how it is done in the Camera Modal Window and do it the similar way.
3. Do not forget on the initialization phase. When the page is loaded you should
update properties on the Manual Control panel and take them from the list.
4. You should handle unsaved changes the same way as the Camera Modal window.

Update the Polygon component. At the moment the behavior is almost as expected except the following scenario: What is OK: The first zoom and pan action is OK. When the user touches the Polygon with two fingers on the mobile device and uses pinch/spread action on the Polygon the image zooms in/out correctly with the center of the zoom in the middle of those fingers. If the user moves fingers across the polygon maintaining constant distance between fingers the image pans/moves relative to the original size of the image from one side of the polygon to the other (for example from the left to the right). This is OK because the zoom pan action started on the original image which was not zoomed nor panned. Therefore at this stage it is correctly related to the absolute state/dimensions of the image.
What is not OK: if the image was already zoomed and panned before the second touch and user again uses two fingers to move from the left of the Polygon to the right maintaining constant distance between fingers the image should move/pan much slower taking into account position of the previously zoomed in and panned image. That means the second zoompan touch of two fingers on the already zoomed in image should use the center point between fingers as the anchor but this time the anchor will be related to the already zoomed in image.

Add a new section into the Cameras -> Manual Control - Input Img Modal Window.
The section will be called "Static Reticle" and it will have the following components:
	X coord: Slider(minValue=-1, maxValue=1, step=0.01, decimalPlaces=4)
	Y coord: Slider(minValue=-1, maxValue=1, step=0.01, decimalPlaces=4)
	color: ColorPicker
	size: Slider(minValue=0.1, maxValue=10, ste=0.1, decimalPlaces=1)
Update Appply action to modify settings
of th master_controller.cameras_controller.cameras[CAMERA_INDEX].image_cropped_resized
and Save button actions to persist the reticle setup in the settings.json

Remove Static Reticle section from the Manual Control - Input Img panel.
The section should be only on the Modal Window.
Modify the Preview on the Manual Control - Input Img and in the Modal window.
If the Preview is enabled the Reticle should be displayed in the Polygon
Use reticle props and set showReticle to true. If the Preview
is not Enabled set showReticle to false.

Update the Polygon component. At the moment the behavior is almost as expected except the following scenario: What is OK: The first zoom and pan action when using mouse wheel is OK. When the user scrolls the Polygon for the first time the image zooms in/out correctly with the center of the zoom in the mouse cursor position. That is OK.
What is not OK: if the image was already zoomed and panned before the second mouse wheel scroll and user moves mouse cursor before he scrolls and then he scrolls the image moves rapidly. It is probably because the anchor point is not calculated correctly. It should take into account position of the previously zoomed in and panned image. That means the second zoom with mouse scroll wheel on the already zoomed in image should use the center point of the mouse cursor as the anchor but this time the anchor will be related to the already zoomed in image.

Update the Polygon component. At the moment the behavior is almost as expected except the following issues:
1. When user uses mouse wheel to zoom in/out the image the page or modal window should not scroll. You should not allow the page to scroll (either entire content or content of modal window) when user uses mouse wheel above a Polygon component which has the zoomAndPan enabled.
2. You should implement the pan functionallity. When user clicks and holds the mouse middle button and moves the mouse cursor the image should pan/move relative to the image in current state (original or already zoomed in and panned). Other functionalities of the Polygon component (such as joystick functionality) should not be affected.

1. In the settings.json rename "ManualControl" to "manualControl". Perform an impact analysis of
this change in the rest api scripts and Cameras.jsx and rename it there as well if necessary.
2. In the settings.json rename "AIControl" to "aiControl". Perform an impact analysis of
this change in the rest api scripts and Cameras.jsx and rename it there as well if necessary.

1. Update the ManualControl web page. There should be a Polygon component filling in entire content area
without any spaces. No padding, no margin. When user resizes the web browser, the Polygon
component should adapt always filling in entire content area. Horizontal and vertical
scrollbars should not be displayed on this page.
2. The Polygon component should be zoomable. It should display live feed from
the master_controller.cameras_controller.cameras[PRIMARY_CAMERA_INDEX].image_cropped_resized.
When the page is closed or user navigates to another page, the live feed should be released.
3. The stretchMode of the Polygon should be set to "fit".
4. There should be reticle displayed
in the Polygon component based on reticle settings of the primary camera
see settings.json -> cameras[PRIMARY_CAMERA_INDEX].ManualControl["static_reticle_..."].
5. Joystick props should be enabled. joystickLineMaxLength should be 0.33.

1. Update the Manual Control web page. Add following behavior:
Add "Setup" button to the right bottom corner of the content area.
Z-index is top most. Add some small margin between bottom and right of the web browser
and the Button. There is no text in the button. There is only a "settings.svg" icon displayed
in the button. The button is 20% transparent.
Add "full screen button" to the left side of the setup button use the same approach as for
the setup button, btu the icon of the button will be fullscreen.svg or
fullscreenExit.svg depending on the situation.
2. If the User clicks on the "full screen" button. The page is switched into the full screen
mode. This behavior should work in the desktop web browser as well as in the mobile
phone web browser. The menu is hidden and the content area (covered with Polygon)
fills entire screen. The icon of the full screen button changes to fullscreenExit.svg.
When user double clicks on the "full screen" button again or when he presses
the escape key on the keyboard (native behavior of desktop web browsers)
the full screen mode is canceled and the top menu appears as usual.

1. Ok. I need to correct myself. The setup button and fullscreen button should not have
topmost z-index. They should just be placed above the Polygon component, but below
the main menu, which appears when the menu button is clicked. The background color
of the setup button and the fullscreen button should be the same as the color
of the menu button in the header.
2. When user clicks on the fullscreen button the header panel with the menu button
remains visible, but it should be hidden and the Polyton should
cover the entire page (while in fullscreen mode).

When user switches to the fullscreen the header (menu bar) which contains
text "Manual Control" and the main "Menu" button is still visible. Take some
effort to figure out, why it is still visible. Fix the behavior.

Create DatePicker component:
The DatePicker will allow users to select a date from the calendar. The DatePicker should have at least following props:
- timeZone - by default set to local time zone
- value - by default set to midnight off current day
- minValue - By default set to 1st of January 1900
- maxvalue - by default set to 31st of December 2199
- format - by default using general notation yyyy-mm-dd
When user selects a date, the value should be midnight/beginning of the selected date in the timeZone. When user enters the DatePicker the calendar should appear in the popup. There are similar rules used for the popup, like they are in the colorPicker. User can either select a date from the calendar or write the date using keyboard or use copy/paste of a text representing the date. The calendar uses modern approach and UX design allowing the user to quickly find the date.
Demonstrate the usage of DatePicker in the Sandbox.

When user clicks on the ComboBox like arrow in the DatePicker the Popup should appear
above the parent Panel. At the moment the popup is trimmed/cropped by the
size of parent panel.

Something went wrong during last change. Now the user can see the calendar above the parent panel.
But when he clicks on it or tries to perform any action, the popup disapears and DatePicker value is not changed.

The SVG icon used in the DatePicker component should be saved into the assets/icons folder and loaded from there.

Rename the DatePicker component to DateTimePicker. There will be a new props named "precission":
If the precission is:
days – in that case the behavior of the component is the same as the behavior of the DatePicker in current state.
hours – in that case the DateTimePicker allows user to select hours as well (take into account daylight saving hour, if the timeZone uses daylight saving hour)
minutes – in that case the DateTimePicker allows user to select minutes as well
seconds – in that case the DateTimePicker allows user to select seconds as well
months - in that case user cannot select specific day, he can select only month
years - in that case user can select only year

Create a new component Joystick1D. The component can be switched between horizontal mode and vertical mode. Joystick1D looks almost like a slider on the audio mixer. Take a look into the assets/ joystick1d.png. Where are three of those Joystick1D in a vertical mode. But imagine it as well in the horizontal position.
The component is rectangular with a big rectangular button, which can be moved like a slider. When user releases it, the button snaps back to the position 0, but that is only animation. The events triggered when user releases the button are onChange(value=0) and onEnd(). User must click on the button and move it. User cannot click on the Joystick1D area and hope the button will move there automatically. He needs to actively move the button. If the user moves mouse cursor or finger out of the joystick area while dragging the button it must not stop the dragging. The dragging should continue until user releases the button even if mouse cursor is not above the Joystick1D. Design of the button is very similar to the buttons in the assets/joystick1d.png
There are following props:
backgroundColor – background color of the Joystick1D area (by default – transparent)
rulerColor – color of the ruler and text in the ruler (by default blue same as used in the HorizontalSeparator)
rulerShowText – true or false. By default true.
rulerLineDistance – by default 0.2.
buttonColor – color of the button. By default blue same as theme of regular buttons.
buttonOutline – color of the button outline and inside grooving. By default blue.
valueOrigin – the position where the button should snap and where it is if the user is not dragging it. By default 0.
value – value telling where the button is rounded to 6 decimal places
minValue – min value of the Joystick1D. By default -1.
maxValue – max value of the Joystick1D. By default 1.
snapAnimationDuration – this value describes how long it takes in seconds to move button from the position where user released it to the valueOrigin position. By default 0.1.
Events:
onStart – triggered when clicks on the button
onChange – triggered when user drags the button and value changes.
onEnd – triggered when user releases the button
Demonstrate usage of Joystick1D in the sandbox

THere are following issues with the Joystick 1D:
The ruler seems to be in front of the button. I can see lines above the button.
The grooving on the vertically oriented button is OK (it is horizontal),
but the grooving on the horizontally oriented button should be vertical.

When user releases the button, the value should be immediately set to valueOrigin
and there should be no more onChange events triggered

Update the Motors Page. On the Test Motor modal window use the Joystick1D
in the Horizontal mode instead of Polygon component.

There is a problem with the Polygon component. Whenever user uses joystick in the Polygon
component while the Polygon is zoomed in, the Joystick does not recognize that and still
uses absolute coordinates instead of relative ones, therefore joystick is drawn in the wrong place

Update the Polygon component. There is a problem with the joystick. When the Polygon is
zoomed in and user clicks on the Polygon component the joystick does not spawn in the position
of the mouse cursor. It does not take into account relative position.
Props like joystickSize and joystickLineMaxLength should take into account relative position.
The joystickSize and joystickLineMaxLength should optically be the same
regardless of if the zoom and pan is used or not.

There is an issue with the Polygon component and the canvas where the "dynamic features" 
(reticle, joystick and polygons) are drawn. Whenever user zooms in to the Polygon
the canvas and all of its "dynamic features" are zoomed in as well which means
that with higher zoom they are getting more pixelated. This should not happen. 
You are most likely drawing the features into the overlay canvas of the same size as the Polygon
and then if zoom and pan is used you are zooming the canvas. That is not good. The overlay
where the "dynamic features" are drawn should always be sharp. Proposed approach:
Overlay canvas will not zoom and pan the same way as the background image, 
but When user zooms and pans
you should recalculate relative positions and sizes of the reticle and polygons
so they optically zoom in and pan. On the other hand
joystickSize and joystickLineMaxLength optically stay the same regardless of zoom and pan
within the displayed Polygon area.

Update the polygon component. Whenever user clicks into polygon and starts to use the joystick
and moves mouse cursor above another component or leaves the polygon component while
still holding left mouse button down the joystick disengages. The joystick should be
released after user releases mouse button.

Update the Manual Control web page. When user clicks on the setup button
the modal window is displayed. On the modal window there are the following components:
Displayed Motors: HorizontalSeparator
MotorBlue: Switch
MotorPurple: Switch
MotorYellow: Switch
MotorOrange: Switch

Update the Manual Control web page, Setup button, Modal window.

1.There should be always at most 4 motor displayed on the Modal window.
The settings.json -> manualControl -> motors section should contain at most 4 items.
If there are more than 4 then ignore them. Only the first four will be displayed in the
Displayed Motors section.
2. The names of motors and the values of the boolean switches displayed on the Modal window
should be loaded from the backend.
Values of the switches are saved in the settings.json -> manualControl -> motors -> enabled. The names
of the motors should be gathered by settings.json -> manualControl -> motors -> index which
is an index of the settings.json -> motors item (you should find the name of the motor there).
3. The switches on the modal form should be optically aligned. If user presses the save button
on the modal window the state of the switches should be saved to 
settings.json -> manualControl -> motors -> enabled.

4. If the first displayed motor is enabled the Joystick1D should be displayed on
the Manual Control web page above the Polygon component.
It should be vertically aligned to the bottom and horizontally
aligned to the middle of the page. The orientation of the Joystick1D should be horizontal.
The color theme of the Joystick1D should be blue.

5. If the second displayed motor is enabled the Joystick1D should be displayed on
the Manual Control web page above the Polygon component.
It should be vertically aligned to the middle and horizontally
aligned to the right of the page. The orientation of the Joystick1D should be vertical.
The color theme of the Joystick1D should be purple.

6. If the third displayed motor is enabled the Joystick1D should be displayed on
the Manual Control web page above the Polygon component.
It should be vertically aligned to the top and horizontally
aligned to the middle of the page. The orientation of the Joystick1D should be horizontal.
The color theme of the Joystick1D should be yellow.

7. If the fourth displayed motor is enabled the Joystick1D should be displayed on
the Manual Control web page above the Polygon component.
It should be vertically aligned to the middle and horizontally
aligned to the left of the page. The orientation of the Joystick1D should be vertical.
The color theme of the Joystick1D should be brown.

8. Avoid following Errors:
8.1 The switches on the Manual Control page in the Setup Modal window are not optically aligned.
Take a look into the Cameras web page Modal Window to see how they are aligned there and
use the same approach.
8.2 User must be able to update the value of the switches.
When he clicks on the switch (at the moment) nothing happens.
8.3 Set the proper name/caption to the button "Save".
8.4 When user clicks on the "Save" button. The web page should call rest api to save the status of the
switches. Create restapimanualcontrol.py file and put the logic there. It may be used as well for
the loading of the switches values including names of the mototrs.
8.5 When user navigates out of the page or if he closes the page
you should terminate processing of the live feed.

Update the manual control web page.
If the first displayed Joystick1D is enabled then use the following logic for its positioning:
The first displayed Joystick1D (Blue) should be vertically aligned to the bottom in the same line
with the fullscreen and setup buttons. It should be horizontally
aligned to the middle of the page. If there is not enough space and
the fullscreen button + margin would cross the bounding box of the
first displayed Joystick1D it should retreat to the left to give the fullscreen button some space and to
avoid collision with the fullscreen button. If the user starts to shrink width of the web browser and
If the retreating Joystick1D touches left side of the web page
(its x position is 0 or near 0) it should jump a little bit more up closer to the top of the page
(idealy on the same y coordinate as it is now) and be horizontally aligned to the middle
 to leave some room for the fullscreen button and for the save button.
 Positioning of other three Joystick1D should not be modified.

There are the following issues with the Joystick1D:
1. Add new props rulerLineWidth. Default value is 1.
2. Add new props rulerVisible. Default value is true. If ruler is not visible then
ruler text is not visible as well.
3. Ruler text at the moment shifts ruler line to which it is assigned (if the Joystick1D orientation is vertical).
That is not OK the text should be on the right side
of the line if Joystick1D orientation is vertical or below the ruler
if the orientation is horizontal. Ruler lines positioning should not be distorted by the text.
4. Add new props socketColor which defines the color of the socket (gap in which
the button moves) displayed behind the button.
5. The props buttonColor does not work. It should change the color of the button behind groovings.
Groovings should be white stripes with increasing alpha in the direction to the middle of the button
like it is right now. Current style of groovings is good.

Update General Setup web page, settings.json and rest api to allow the application
to load and save new properties. There will be a new panel on the General Setup
web page called "Essentials" containing following components:
- Latitude (degrees): Slider(minValue=-90, maxValue=90, step=1, decimalPlaces=1)
- Longtitude (degrees): Slider(minValue=-180, maxValue=180, step=1, decimalPlaces=1)
- Altitude (meters): Numeric input
(min=0, max=15000, minSliderValue=0, maxSliderValue=4500,step=1, decimalPlaces=0)
- Center point true bearing: Slider(minValue=0, maxValue=360, step=1, decimalPlaces=0)
- Center point elevation: Slider(minValue=-90, maxValue=90, step=1, decimalPlaces=1)
All components should be optically aligned. Take a look into the Cameras web page Modal window
to see how to do optical alignment.

Update Motors web page Modal Window. Following properties should be hidden
on the modal window and as well on the panel:
- Enabled
- Type
- Role
Those properties should still be in the settings.json and in the rest api.
Only on the GUI they will be hidden.

Add following Motor properties to the Modal Window, to the Panel, to the settings.json
and to the rest api to allow the application to load and save the properties:
- Stroke length (cm): Slider(min=0, max=1000, minSlider=1, maxSlider=100, step=0.5, decimalPlaces=1).
This input should be placed below "Inertia" slider (on the Modal Window and on the Panel)
but only in case the role of the motor is "leftArm" or "rightArm", otherwise this slider
should not be visible.
- PWM frequency (kHz): Slider(min=0, max=16, step=1, decimalPlaces=0).
This input should be placed above "Histogram" table in the
Histogram section (on the Modal Window and on the Panel)
- Forward Enable: ComboBox with a list of pins.
This input should be placed below "Forward" Combobox in the Pins section.
- Reverse Enable: ComboBox with a list of pins.
This input should be placed below "Reverse" Combobox in the Pins section.

1. After second thought add stroke length to all Modals and all Panels on the Motors page.
2. Make sure the "Forward Speed Histogram" chart and "Reverse Speed Histogram" chart are properly aligned in the panel (have proper margin). Currently they are touching left side of the panel.They should have the same margin as a "Test Motor" button and the same width to fill the panel.
3. The x axis of the "Forward Speed Histogram" chart and "Reverse Speed Histogram" chart should be renamed to "Speed %" and the value should be multiplied by 100.
4. Instead of the svg image currently displayed in the header of the panel, there should be a circle of the same size as the image filled with solid color.
The color for each motor is in the settings.json -> motors[INDEX].color. Update the rest api if there is a need to update it.

1. Update Joystick1D component. Add new props "mode". Mode can be:
- "joystick" - same behavior like now. When user releases joystick the snap animation kicks in
- "slider" - similar behavior like joystick. The events (onChange, onEnd) are triggered
the same way like in the "joystick" mode but the last onChange have as a value
the position of the button (not 0) and the button does not snap back to zero position.
It remains where user left it. User needs to click on the button and move it back manually.

2. Update Manual Control web page. We need to redesign the Modal Window. The modal window
should contain four sections for first four motors which are mentioned in settings.json -> manualControl.motors
All of them are named "Motor / Device". In each
section there will be following components:
Name: PlainText(value=NAME_OF_THE_MOTOR) - gathered from settings.json -> motors[INDEX].name
Enabled: Switch. Same behavior like it is on the modal at the moment.
Mode: ComboBox(values=["joystick", "slider"]) - Sets up the joystick mode. This ComboBox is enabled
only if the Enabled switch is on. Value of this combobox is stored in
settings.json -> manualControl.motors[INDEX].mode. Update rest api if necessary.

3. Update Manual Control web page. Color of each joystick should be loaded from
settings.json -> motors[INDEX].color

Update Motors web page. When user clicks in the Histogram Quick Test on
the Motor Action MultiSwitch (for example on Forward button) and then directly clicks
on the Reverse button both pins are active. This should not happen. If user goes from forward directly to reverse
or from reverse directly to forward the stop action should be trigerred automatically in between.

Update Motors web page. Update business validations on the Modal Window.
Currently when a Forward or Reverse pin is selected the application performs
a business validation checking if the pin is not used by other motor or by the same motor in oposit direction.
This validation is "blocking" preventing user from saving such Modal window if there is a collision.
1. The validation should be non-blocking.
2. The validation should be extended. Pin "Forward Enable" and "Reverse Enable" should
be included in the validation. If user selects a Forward Enable pin or Reverse Enable
pin which is used anywhere else a warning button should appear.

It is still not OK. I will give you examples:
1. If I select Forward Pin = 8 and Reverse Pin = 8 - at the moment
the ComboBoxes are highlighted triggering a blocking situation.
The comboboxes should be highlighted but it should trigger non-blocking situation.
2. If I select "Forward" pin the same as "Forawrd Enable" pin currently nothing happens.
Both ComboBoxes shold be highlighted. It shold trigger non-blocking situation.
3. If I select "Forward Enable" pin the same as "Reverse Enable" pin currently nothing happens.
Both ComboBoxes shold be highlighted. It shold trigger non-blocking situation.

Basically if user selects a pin in "Forward", "Forward Enable", "Reverse", 
"Reverse Enable" which appears in any other ComboBox in any other motor
or in the same motor the Warning button should be visible. The only exception
is 0 Dummy GPIO pin. This pin can be selected multiple times within
the same motor or acros multiple motors.

It is still not OK.
1. On the Modal window you need to check if "Forward" is not the same as
"Forward Enable", "Reverse", "Reverse Enable" of the same motor. You as well need to check
if "Forward" is not the same as "Forward", "Forward Enable", "Reverse", "Reverse Enable" of other motors.
2. On the Modal window you need to check if "Forward Enable" is not the same as
"Forward", "Reverse", "Reverse Enable" of the same motor. You as well need to check
if "Forward Enable" is not the same as "Forward", "Forward Enable", "Reverse", "Reverse Enable" of other motors.
3. On the Modal window you need to check if "Reverse" is not the same as
"Forward", "Forward Enable", "Reverse Enable" of the same motor. You as well need to check
if "Reverse" is not the same as "Forward", "Forward Enable", "Reverse", "Reverse Enable" of other motors.
4. On the Modal window you need to check if "Reverse Enable" is not the same as
"Forward", "Forward Enable", "Reverse" of the same motor. You as well need to check
if "Reverse" is not the same as "Forward", "Forward Enable", "Reverse", "Reverse Enable" of other motors.

If the check detects a violation of business validation (described above) it should be non-blocking.
The only exception to the checks described above is 0 Dummy GPIO pin.
This pin can be selected multiple times within
the same motor or acros multiple motors.

Create new invisible/renderless component called Timer. The Timer has the following props:
	enabled - identifies if the timer is ticking and can trigger events or not
	interval - the interval in seconds. Default value is 1
The component has following props:
	onStart - triggered when the enabled props is set to true
	onInterval - triggered every time when interval elapses
	onEnd - triggered every time when enabled props is set to false or when the component is enabled and 
will be destroyed/deleted.
Demonstrate the usage of the component in the Sandbox -> ComponentsDemo

Update the Manual Control web page. There should be a Timer component with interval 0.25 second which calls
following function:
Whenever time interval ticks the frontend sends a json request to the backend via rest api
using "post" method "/api/manualcontrol/action". The request json should look like this:
{
	"fullscreen": false, //is user using fullscreen or not
	"joystick"{
		"x": 0.0, //x position of the Polygon joystick relative to the joysitck origin. If Polygon Joystick is not spawn then 0.0.
		"y": 0.0, //y position of the Polygon joystick relative to the joystick origin. If Polygon Joystick is not spawn then 0.0.
	}
	//all motors displayed on the Modal window should be mentioned in the motors section
	//typically there are 4 items
	"motors":[
		{
			"index": 2, //index of the motor
			"value": 0.0 //value of the Joystick1D, it does not matter if the joystick is visible or not
		},
		{
			"index": 3, //index of the motor
			"value": 0.0 //value of the Joystick1D, it does not matter if the joystick is visible or not
		},
		{
			"index": 4, //index of the motor
			"value": 0.0 //value of the Joystick1D, it does not matter if the joystick is visible or not
		},
		{
			"index": 5, //index of the motor
			"value": 0.0 //value of the Joystick1D, it does not matter if the joystick is visible or not
		},
	]
}
The backend will process the request. It will set up the speed of motors with index 0 and index 1
using "move" method based on the "joystick" "x" and "y" position from the request. It will as well
set up the speed of motors mentioned in the section "motors". and it will send response
as a json in the following format:
{
	"success": true,
	"motors":[
		{
			"index": 0, //motors 0 and 1 will be as well included in the response
			"position": 0.0,
			"speed": 0.0,
			"duty": 0.0
		},
		{
			"index": 1, //motors 0 and 1 will be as well included in the response
			"position": 0.0,
			"speed": 0.0,
			"duty": 0.0
		},
		{
			"index": 2, //motors 0 and 1 will be as well included in the response
			"position": 0.0,
			"speed": 0.0,
			"duty": 0.0
		}
	]
}
The same method which is triggered during the Timer onInterval event is triggered as well
when user changes the position of the joystick on the Polygon component.

sendManualControlAction must be called synchronously meaning 
the system should wait for the response or at least 5 seconds till the new request is sent.
It does not matter if the latest sendManualControlAction was triggered by dragging joystick or by the timer
all other sendManualControlActions are discarded meaning the function does not
call backend and returns (latest known result) immediately.

Update the HorizontalSeparator. Add a new props "help". The help is a link to a web page. If the help is specified then
the text (label) in the HorizontalSeparator will have a postfix " &nbsp; &#10914; &nbsp; more info".
The "more info" text will be a link to the "help" page. When the user clicks on the link the page opens
in a new panel/window.

Create Camera2 class in camera2.py which takes camera pictures and saves it to the _frame.
The logic should be like this. The camera is created with an "index" parameter
identifying the input device.
By default the device is closed.
When user accesses "frame" property the Camera2 checks if the thread is running. If it is not then
it will start the thread and immediately return black frame of size 640x480.
The thread captures pictures of the camera as quickly as possible and saves them to variable _frame,
which is returned if the user accesses frame again,
but the thread stops if user does not accessed "frame" property for more than 5 seconds.
That way it is ensured, that if the user accesses frame in a rapid succession
he will get it as quickly as possible. But if user does not access "frame" for more than 5 seconds
the thread will close the device and stop, till the next access of the "frame" property.
Then the thread will be started again. It will open a device and take images from camera.

Add properties width, height, and fps to Camera2. Implement getters and setters.
If the user gets property value the system should check if the thread is running and the device
is opened, if it is system returns property value (for width it is cv2.CAP_PROP_FRAME_WIDTH, for height
it is cv2.CAP_PROP_FRAME_HEIGHT and for fps it is cv2.CAP_PROP_FPS). If it is not, it will open
the device and start the thread, because we can assume the user will want to get other
properties in rapid succession. Thread will then automatically close the device after 5 seconds.
If the user wants to set property, the system should check if the thread is running and the device
is opened. If it is opened the system should safely set property. If it is not the system
should open the device and start the thread, because we can assume the user will want to get other
properties in rapid succession.

Add properties into the Camera2 in the same way like the widht and height is done.
The list of device properties is following:
bitrate
buffer_size
brightness
contrast
hue
saturation
sharpness
gamma
white_balance_temperature
backlight
gain
focus
exposure
auto_white_balance_temperature
auto_focus
auto_exposure

Implement as well special properties:
flip_horizontal
flip_vertical
rotate

Avoid mistakes in camera.py. Do it the right way.

Implement NodeImage, NodeImageCroppedResized, NodeImageAI in camerapostprocessing2.py.
The NodeImage has a "frame" property which is the same as _parent.frame (parent Camera2 frame).
It has a "settings" property. There is no need to have fps property. Even if NodeImage
is a class of type Thread there is no need to start the Thread since the "frame" is returned
directly from parent Camera2.
NodeImageCroppedResized has properties crop_top, crop_left, crop_bottom, crop_right,
width, height, static_reticle_x, static_reticle_y, static_reticle_color, static_reticle_size.
Only getters of those properties are implemented. If user wants to set those properties
he needs to use "settings" property to set all of them.
if crop_top, crop_left, crop_bottom, crop_right, width and height are all less or equal to epsilon
then "frame" property will return directly the "frame" from parent Camera2. Therefore in this case
there is no need to start the thread. On the other hand If one
or more properties mentioned above (crop_top, crop_left, crop_bottom, crop_right, width, height)
are higher than epsilon and user requests a frame
then the same logic as implemented in Camera2 has to be applied: the Thread has to be started
thread will gather Frame from parent Camera2 (in the infinite loop).
If the "time" property of "frame" of parent
Camera2 is different from last processed "time" of "frame" of NodeImageCroppedResized then
the thread will apply crop and resize image post processing
to the "frame" of parent "Camera2" and store it as local
_frame. The thread runs at least for 5 seconds after user requested last "frame" from
the NodeImageCroppedResized.
There is no special logic regarding "static_reticle" those properties are only saved to settings.

Do not implement nodeaAi at this moment.
Avoid mistakes in camerapostprocessing.py. Do it the right way.

Create NodeImageAI in camerapostprocessing2.py. There will be following props: model,
model_name, mask_polygons. Use similar approach as with NodeImageCroppedResized
but this time the Thread is started every time when user accesses the "frame"
property and it is stopped after 5 seconds, if user does not accesses the "frame" property.

Strategy - how the frame is processed in the infinite loop is following:
If mask polygons does not contain any data then do not apply mask layer on the parent Camera2.frame.
That means the masked_frame is the same as Camera2.frame.
If mask polygons contains some data then create an overlay mask layer which is filled
with black color and only polygon areas are transparent (have alpha=0). This
overlay mask layer is then put on top of the Camera2.frame before
the frame is processed by the model. This masked frame is called "masked_frame".
As soon as the masked_frame is processed by the model the resulting points are drawn into
the "label" layer. Important points mean points like shoulders, ears, nose, ...
The label layer is then drawn with 50% transparency on top of masked_frame and used as 
a final result for the _frame.

Update the code of camerapostprocessing2.py. The mask_polygons are passed in
the settings in following format. Mask polygons is a list of a list of Vector2D
where x=0.0 and y=0.0 means left top corner of the image and x=1.0, y=1.0 means the right
bottom corner of the image. The mask polygons may look like this:
"mask_polygons": [
	[
		{"x": 0.0, "y": 0.0},
		{"x": 1.0, "y": 0.0},
		{"x": 1.0, "y": 0.3},
		{"x": 0.0, "y": 0.3},
	]
]

Convert getter and setter properties in camera.py from line 267 to line 522.
Convert getter properties from this (template):
	@property
    def bitrate(self) -> float:
        """Get the camera bitrate."""
        self._ensure_device_open()
        with self._lock_property_manipulation:
            if self._active and self._camera is not None:
                return self._camera.get(cv2.CAP_PROP_BITRATE)
			return -1.0

to this (template):
	@property
    def bitrate(self) -> float:
        """Get the camera bitrate."""
        with self._lock_property_manipulation:
            self._open()
            if self._active and self._camera is not None:
                result = self._camera.get(cv2.CAP_PROP_BITRATE)
            else:
                result = -1.0
            self._close()
            return result

Convert setter properties in camera.py from this (template):
	@bitrate.setter
    def bitrate(self, value: float):
        """Set the camera bitrate."""
        self._ensure_device_open()
        with self._lock_property_manipulation:
            if self._active and self._camera is not None:
                self._camera.set(cv2.CAP_PROP_BITRATE, value)

to this (template):
	@bitrate.setter
    def bitrate(self, value: float):
        """Set the camera bitrate."""
        with self._lock_property_manipulation:
            self._open()
            if self._active and self._camera is not None:
                self._camera.set(cv2.CAP_PROP_BITRATE, value)
            self._close()

Update the Camreas web page. The first modal window should be extended by following components
placed below the "exposure" Slider just above the "preview" HorizontalSeparator:
Crop: HorizontalSeparator - this section and components in this section have the same behavior/setup like "Crop" section of the second Modal window
	Top % (0 pixels): Slider
	Left % (0 pixels): Slider
	Bottom % (0 pixels): Slider
	Right % (0 pixels): Slider
Stretch: HorizontalSeparator - this section and components in this section have the same behavior like "Stretch" section of the second Modal window
	Width: NumericInput
	Height: NumericInput
Static Reticle: HorizontalSeparator - this section and components in this section have the same behavior like "Static Reticle" section of the second Modal window
	X coord: Slider
	Y coord: Slider
	Color: ColorPicker
	Size: Slider
The settings.json was updated and all properties mentioned above are included
in the settings.json -> cameras.
Update the restapicameras and CameraSettings class if necessary. The new settings properties
mentioned above should be loaded and saved to the settings.json via backend.

Update the Cameras web page. The "preview" Polygon on the first Modal window should display reticle
based on the setup in the "Static Reticle" section. It should as well display polygons (if there are any)
in the json returned by the api/cameras/list and allow user to modify/add/delete polygons on the Polygon component.
Polygons should be saved when user clicks on the "Save" button.

1. Remove second and third panel including their modal windows from the cameras web page.
2. Update the rest api and cameras web page. Settings.json and camera section of api/cameras/list
should have the following structure (there will be no "general", "manual_control" or "ai_agent" sections):
"cameras": [
	{
		"index": 0,
		"name": "0: GUID_1.0",
		"camera_role": "scope_ai_control" or "scope_manual_control" or "spotter_surveilence_cam",
		"supported_resolutions": [
			{
				"width": 320,
				"height": 240,
				"label": "320 x 240"
			},
			{
				"width": 640,
				"height": 480,
				"label": "640 x 480"
			},
			{
				"width": 848,
				"height": 480,
				"label": "848 x 480"
			},
			{
				"width": 960,
				"height": 540,
				"label": "960 x 540"
			},
			{
				"width": 1280,
				"height": 720,
				"label": "1280 x 720"
			},
			{
				"width": 1920,
				"height": 1080,
				"label": "1920 x 1080"
			}
		],
		"flip_horizontal": false,
		"flip_vertical": false,
		"rotate": 0,
		"crop_top": 0.0,
		"crop_left": 0.0,
		"crop_bottom": 0.0,
		"crop_right": 0.0,
		"stretch_width": 0,
		"stretch_height": 0,
		"static_reticle_x": 0.0,
		"static_reticle_y": 0.0,
		"static_reticle_color": "#ff0000cc",
		"static_reticle_size": 0,
		"mask_polygons": [],
		"model_name": "yolo11n-pose",
		"width": 640,
		"height": 480,
		"fps": 30,
		"bitrate": -1,
		"buffer_size": 1,
		"brightness": 128.0,
		"contrast": 32.0,
		"hue": 0.0,
		"saturation": 64.0,
		"sharpness": 3.0,
		"gamma": 120.0,
		"white_balance_temperature": -1.0,
		"backlight": 1.0,
		"gain": -1.0,
		"focus": -1.0,
		"exposure": -6.0,
		"auto_white_balance_temperature": true,
		"auto_focus": true,
		"auto_exposure": true
	}
]
3. There is a new property called "camera_role". Camera role can have one of the following values:
"scope_cam_ai" or "scope_cam_manual_control" or "spotter_cam_surveilence". It is not displayed
on the modal window and it cannot be changed. It is only passed in the json when saving camera.
For the first panel of the page it will always be "scope_cam_ai".

Update the Polygon component. In case there is a polygon displayed in the Polygon component 
there is a problem when zooming in and panning. The points are zoomed as well and the thickness of the
lines connecting points is enlarged as well. This must not happen. The correct behavior is following:
When user zooms in
point size and line thickness should remain constant. The position of points should be relative
to the zoom and pan.

I want to ask you a question. Do not code anything. Only answer the question.
There are two special sections in the settings.json "camera" and "primaryCamera"
see the code below:
"camera": {
	"source": "cam1",
	"flipHorizontally": false,
	"flipVertically": false,
	"rotate": "0",
	"cropLeft": 0,
	"cropTop": 0,
	"cropRight": 0,
	"cropBottom": 0,
	"resizeEnabled": true,
	"resizeWidth": 640,
	"resizeHeight": 480,
	"showReticle": true,
	"reticleX": 50,
	"reticleY": 50,
	"reticleColor": "#ff0000cc",
	"reticleSize": 1
},
"primaryCamera": {
	"index": 0,
	"name": "0: GUID_1.0"
}
Is settings.camera or settings.primaryCamera used anywhere in
any python script or javascript? If yes, tell me where?

Rename "Camera(s)" web page to "Camera(s) old". Rename Cameras.jsx to Cameras_old.jsx.
Rename restapicameras.py to restapicameras_old.py. Rename all /api/cameras/... end points to /api/cameras_old/...
Make sure web page works correctly after the renaming is finished.

Create a new web page "Cameras.jsx" and the backend api "restapicameras.py". We need to build this web page
and the backend api from scratch because we have made several mistakes last time in the Cameras_old.jsx
and in the restapicameras_old.py.
We are no longer using "primaryCamera" in the settings.json and the "cameras" structure in the settings.json
was redesigned in the following way. Now it has three items (camera_codes):
- scope_cam_ai
- scope_cam_manual_control
- spotter_cam1

Create three panels on the new web page "Cameras.jsx". Names of those panels will be:
- Scope Camera - AI
- Scope Camera - Manual Control
- Spotter Camera

There will be following components on each panel:
Camera Settings: HorizontalSeparator
	Device Index: TextField - typically a number
	Resolution: TextField - typically a text in a format "width x height"
	FPS: TextField - typically a number
Basically it will be very similar to the first section of the panel in Camera_old.jsx
The page should call the backend API "api/cameras/list" to get those values for all three panels
from the settings.json.

This time the backend API will have only the following endpoints:
- api/cameras/list - will return cameras object from the settings.json -> cameras (three items, scope_cam_ai, ...)
- api/cameras/update/{camera_code} - will update the cameras object in the settings.json -> cameras
and will call "reset" of MasterController.CamerasController
- api/cameras/input_devices - will return a list of available input devices. Basically it will get information from
MasterController.CamerasController.cameras[index].settings
- api/cameras/stream/{item_code} - item_code can be "scope_cam_ai" or "scope_cam_manual_control" or "spotter_cam1"

There will be an edit button in the header of each panel. When user clicks on the edit button the Modal window
will appear. It will look almost exactly like modal window on the Cameras_old.jsx, and the behavior 
will be almost the same except this time it will
utilize new backend API and it will start preview automatically. If the user changes camera source
or if he modifies some properties and clicks on the "Apply" button the preview will restart automatically.

Update the Modal window on the Cameras web page. When the "Save" button is pressed
the preview stream should be stopped. There should be a "Saving..." text
displayed in a similar way how it is done on the GeneralSetup page when user clicks on the "Save" button.
The "Save" button should be disabled as well as the "Apply" button during the saving period. And then the modal window
should be closed. When the Apply button is pressed the preview stream should be stopped, then the
"Apply" button text should be switched to "Applying ..." and the button should be disabled for the
period of applying the setup together with "Save" button. The frontend should call the backend
api/cameras/update endpoint, but this time the Modal window is not closed. As soon as the update
is finished the "preview" stream should be started again.

When the Modal window is closed either by clicking on the "Save" button or by clicking on the "Cancel" -> "Yes" button
the preview stream should be stopped.

Add a "Reset Camera" button beyond the FPS input on the Modal window of Cameras web page.
When the "Reset Camera" button is pressed the preview stream should be stopped. "Save" and "Apply" buttons
should be disabled. The behavior is then is following. The font end calls rest api
api/cameras/reset/{camera_code}. The backend then sets 
master_controller.cameras_controller.cameras[INDEX].settings = master_controller.cameras_controller.cameras[INDEX].default_settings.
and sends a {"success": true} information to the frontend. Front end then call rest api again
to list all cameras and all input_devices and then it can update properties of the current camera in the modal window.
Finally the front end starts the preview stream again.

Update following files:
	settings.json
	restapicameras.py
	Cameras.jsx
	... ask me if there is a need to update others

The task is following:
- Remove "scope_cam_manual_control" from settings.json. Remove panel "Scope Camera - Manual Control"
with its modal window and remove this option as well from the restapicameras.json.
- Rename "scope_cam_ai" in the settings and in the restapicameras.json and possibly in the Cameras.jsx to "scope_camera".
- Rename "spotter_cam1" in the settings and in the restapicameras.json and possibly in the Cameras.jsx to "spotter_camera".
- On the Cameras web page in the modal window add the "Stretch Enabled": Switch into the "Stretch" section above
the "Width" NumericInput. Add "stretch_enabled" property to the settings.json and make sure it is persisted when
user modifies it in the Modal window and clicks on "Apply" or "Save" button.
- Add a business validation on the Modal window which checks that the Device Index of "Scope Camera" is different
than the Device Index of the "Spotter Camera". If it is not, the user will not be able to click on
the "Apply" button or "Save" button.

Update the Camera web page Modal window. When user changes "Input Device" the properties on the Modal window
should be updated based on the latest restapi call api/cameras/input_devices[SELECTED_DEVICE_INDEX]

- Update the camera web page. If user changes "Input Device" change the background image in the Polygon component
to cameraOff.svg and do not start cameras/stream.
- If user closes the Modal window either by clicking on "Close" button or "Save" button change the background image
in the Polygon component to cameraOff.svg.

It is not enough, if user clicks on the "Cancel" button the Polygon image should be changed to cameraOff.svg
as well to stop receiving live stream.

Update the Cameras web page Modal window
1. Rename the "Cancel" button to "Close" button.
2. When the user clicks on the "Close" button change the text to "Closing..." and wait for one second. That would give the modal
window chance to change the Polygon image to cameraOff.svg and stop playing live stream.

1. Update the Cameras web page Modal window. When the "Resolution" or "FPS" is changed the Polygon image should be changed to cameraOff.svg.
2. Add one second delay as well to the "Apply" button and "Save" like

Update the Camera web page Modal window. If the "Stretch Enabled" is off then "Width" and "Height" NumericInputs are
disabled as well. If the "Stretch Enabled" is on then "Width" and "Height" NumericInputs are enabled.
The min value of "Width" is 64 the max value is 8192, the min value of "Height" is 64 the max value is 4608.

Write a python function mask_image which takes following parameters image: np.ndarray and mask_polygons: list[list[dict[str, float]]]. Polygons array look like this:

"mask_polygons": [
                [
                    {
                        "x": 0.8924501859224759,
                        "y": 0.7624881233584847
                    },
                    {
                        "x": 0.862535656007946,
                        "y": 0.6884140492844106
                    },
                    {
                        "x": 0.7898860833583734,
                        "y": 0.6371319980023593
                    },
                    {
                        "x": 0.7044159978882879,
                        "y": 0.6029439638143251
                    },
                    {
                        "x": 0.674501467973758,
                        "y": 0.6029439638143251
                    },
                    {
                        "x": 0.6616809551532452,
                        "y": 0.6314339923043536
                    }
                ],
                [
                    {
                        "x": 0.2599715534438435,
                        "y": 0.870750231620593
                    },
                    {
                        "x": 0.41381770728999734,
                        "y": 0.870750231620593
                    },
                    {
                        "x": 0.44373223720452726,
                        "y": 0.7226020834724448
                    },
                    {
                        "x": 0.31552710899939906,
                        "y": 0.642830003700365
                    },
                    {
                        "x": 0.21296300643529648,
                        "y": 0.7169040777744391
                    },
                    {
                        "x": 0.1915954850677751,
                        "y": 0.8194681803385416
                    },
                    {
                        "x": 0.20014249361478365,
                        "y": 0.8878442487146101
                    }
                ]
            ]
The x and y coordinates in the mask_polygons describe position from the top left corner going from left to right (0 -> 1) and from top to down (0 -> 1). The function returns an image and blackens out everything which is not inside polygons. If the mask_polygons is empty then the function returns the image.

Update restapicameras and Camera web page. Add new rest api endpoint "api/cameras/stop/{camera_code}". The endpoint
will load setup of the "camera_code" from settings.json and it will take the index of the camera and
call master_controller.cameras_controller.camera[INDEX].stop(). It will return {"success": true}
Update the Modal window of the Cameras web page and call stop always immediatelly after the "Close" button is
pressed, "Apply" button is pressed and "Save" button is pressed - always before the delay.

Update Camera web page Modal window. Add following components into the "preview" section above
the Polygon component:
- Masked Area Color: ColorPicker(defaultValue="00ee0055")
- HTTP Stream Quality: Slider(minValue=35, maxValue=100, defaultValue=80)
- Display Mode: MultiSwitch(values=[{"Raw": 0}, {"Masked": 1}, {"Masked + AI": 3}], defaultValue="Raw")
Behavior:
When user changes the "Masked Area Color" the Polygon "fillColor" property is set to the newly selected color and
the "borderColor" property is set to the selected color except the alpha channel which should remain FF (not transparent).
The "HTTP Stream Quality" and "Display Mode" should be used as the optional parameter when calling api/cameras/stream
probably in the following way api/cameras/stream/{camera_code}?mode=0&quality=80. Version could be in the URL as well.
Persistence:
None of the parameters mentioned above is persisted in the settings.json. They exist only on the Modal Window.

When user changes "HTTP Stream Quality" or "Display Mode" the change must not be immediate.
The system should display cameraOff.svg in the Polygon and wait for user to press "Apply" button or "Save" button.

Update Cameras web page Modal window. When user click on the "Reset Camera" button a modal window with a question should
appear asking user if he really wants to reset the Camera. Only after user clicks on the yes button
the camera should reset.

Update ManualControl.jsx. The old api endpoint - api/cameras_old/(primary, ...) does not work. Migrate to new api
api/cameras/(list, stream, ...). Take a look how it should be done into restapicameras.py a Cameras.jsx.

1. Update the ManualControl web page Modal window. Add "Cameras" section above "Motor/ Device" section. There will be following
components in the section:
	Camera: MultiSwitch(values["Scope Camera", "Spotter Camera"], defaultValue="Scope Camera")
	HTTP Stream Quality: Slider(minValue=35, maxValue=100, defaultValue=80)
	Scope Camera: MultiSwitch(values=[{"Raw": 0}, {"Masked": 1}, {"Masked + AI": 3}], defaultValue="Raw", enabled=true)
	Spotter Camera: MultiSwitch(values=[{"Raw": 0}, {"Masked": 1}, {"Masked + AI": 3}], defaultValue="Raw", enabled=false)
All components should be optically aligned. Take a look how it is done in Cameras web page on Modal windows.
2. When user changes any of the parameters above the background image of the Polygon component should be
set to cameraOff.svg.
3. When user changes the Camera and selects "Spotter Camera" the MultisSwitch 
"Spotter Camera" should be enabled and the MultiSwitch "Scope Camera" should be disabled.
4. When user changes the Camera and selects "Scope Camera" the MultisSwitch 
"Scope Camera" should be enabled and the MultiSwitch "Spotter Camera" should be disabled.
5. When user clicks on the "Save" button the api/cameras/stop should be called. System should wait 100 ms
and the "Save" button text should be changed to "Saving...". The system saves all four
properties to the settings.json (update restapimanualcontrol.py and settings.json if necessary).
Finally the video stream should be restarted.
6. Since the "Cameras" section properties are persisted in the settings.json - there is a need to update
the behavior of the page when it is displayed (for the first time). The stream setup
(camera_code, mode, quality) should be taken from the settings.json.
7. When user leaves the ManulControl web page (either by navigating to another web page
in the menu or by closing the web browser panel) the rest api api/cameras/stop should be called

1. Update ManualControl web page Modal window. Remove "Preview" section and all of its logic.
2. When user modifies any value in "Cameras" section of the Modal window the Polygon behind the Modal window
should display cameraOff.svg and stop displaying live stream.
3. When user leaves the ManulControl web page (either by navigating to another web page
in the menu or by closing the web browser panel) the rest api api/cameras/stop should be called,
but before the api/cameras/stop is called the image of the Polygon component should
be switched to cameraOff.svg.

Update ManualControl web page. There are still problems:
1. When user clicks on the "Save" button the api/cameras/stop should be called. System should wait for a {"success": true}.
then the system should wait another 100 ms. Save property values of the Modal window and start receiving stream
into the Polygon component by displaying api/cameras/stream
2. When user leaves the ManualControl web page (by navigating to another web page
in the menu) the image of the image in the Polygon component should
be switched to cameraOff.svg. Then the rest api api/cameras/stop should be called. System should wait
for a {"success": true}. Then the system should wait another 100ms and after that the web page can be routed
to another web page.
3. When user leaves the ManualControl web page (by closing the web browser panel)
the image of the image in the Polygon component should
be switched to cameraOff.svg. Then the rest api api/cameras/stop should be called. System should wait
for a {"success": true}. Then the system should wait another 100ms and then the panel can be closed.

Update ManualControl web page. When user is on the ManualControl web page and he clicks on the "Menu" button
in the top right corner in the page header. The Polygon component src should be switched to
the cameraOff.svg and should streaming should stop by calling api/cameras/stop.
The stream should be restarted only in case the user
stays on the page (if he does not navigate to another web page in the menu) and close the main menu.

Update ManualControl web page. Remove the following feature. 
1. When user clicks on the Menu button
and the menu is displayed the Polygon src is changed to cameraOff.svg. This should be removed, but
if user navigates to some other page the camera stop functionality should remain as is.
2. When user modifies any property in the Camera section in the Modal window, the Polygon src is changed to cameraOff.svg.
This should be removed. The camera should play the stream in the background.

1. Update cameraai get_pose_dict to YOLO v11. It was originaly written for YOLO v8, but in v11 there is a different
shape of the ndarray with keypoints. It should be of shape (1, 17, 3). It can detect multiple people.  
2. Focus on performance and put "if" branches before "for" loops.

Update the Polygon component static reticle. Add a new props "reticleOutlineColor". Default value will be #009900cc.
Reticle cross will be outlined by a line of width 1px drawn with the "reticleOutlineColor". The point in the middle
of the cross will not be outlined.

1. Change the width of the reticle outline to the following logic. Outline width should be max(0.5px, 0.5 * size of the width
of the reticle line).
2. Double the gap between reticle lines and the center point, that means move those lines slightly more far always
from the center point.

Update the Polygon component. There is a problem when user zooms into the picture using mouse scroll wheel
or with two fingers on the mobile phone and then tries to pan to the side (left or right) he cannot reach them.
He can see what is happening on the sides only after he completely zooms out. Why is it happening.
Fix the issue.

Update Cameras web page, settings.json and restapicameras.py. Update the Modal window. Add
"Outline": ColorPicker into the "Static Reticle" section below "Color" component
and let the system persist it.

Modify Hot Zone. Move the "Edit" button above the "Reset" button. Width of "Edit" button should be the same
as width of "Reset" button. Update the Modal window.
Sliders in the modal window in each section separately should be optically aligned.

Update AI Behavior web page. Rename "AI Behavior" to "AI Setup". There should be three panels: "AI Essentials", "Mission Rules",
"Exit Strategy". There is an Edit button in the header of the panel. When the Edit button is pressed the modal window
will be displayed and the user will be able to modify values. Each Modal window will be placed into separate jsx file.

On the "AI Essentials" panel there are following components:
	Engagement Essentials: HorizontalSeparator
		Minimum FPS to Allow Engagement: TextField - this value is edited on the Modal Window
		Organ must exist for at least (sec.): TextField - this value is edited on the Modal Window
		Organ Existence - Radius From Reticle (px): TextField - this value is edited on the Modal Window
	AI Validity Period: Horizontal Separator
		Next AI Start (UTC): TextField

On the AI Essentials Modal Window components:
	Engagement Essentials: HorizontalSeparator
		Minimum FPS to Allow Engagement: Slider(min=0, max=50, defaultValue=0, step=1)
		Organ must be detected for at least (sec.): Slider(min=0, max=2, defaultValue=0, step=0.05)
		Detection Radius - From Reticle (px): Slider(min=0, max=10000, maxSlider=2000 defaultValue=50, step=10)
	AI Disable Period: Horizontal Separator
		Disable AI During Periods: Table(columns=["Disabled From (UTC), Disabled To (UTC)],
										 canAddRows=true, canDeleteRows=true, canAddColumns=false, canRemoveColumns=false)
	Organs / Body Parts: Horizontal Separator
	Brain: Horizontal Separator:
		Detection Enabled: Switch(defaultValue=true)
		Size Multiplier: Slider(min=0.2, max=1.0, defaultValue=1.0, step=0.1)
		Confidence Threshold: Slider(min=0.01, max=1.0, defaultValue=0.5, step=0.01)
		Organ Minimum Radius: Slider(min=1, max=10000, maxSlider=200, defaultValue=1, step=1)
	Chest: Horizontal Separator:
		Detection Enabled: Switch(defaultValue=true)
		Size Multiplier: Slider(min=0.2, max=1.0, defaultValue=1.0, step=0.1)
		Confidence Threshold: Slider(min=0.01, max=1.0, defaultValue=0.5, step=0.01)
		Organ Minimum Radius: Slider(min=1, max=10000, maxSlider=200, defaultValue=1, step=1)
	Heart: Horizontal Separator:
		Detection Enabled: Switch(defaultValue=true)
		Size Multiplier: Slider(min=0.2, max=1.0, defaultValue=1.0, step=0.1)
		Confidence Threshold: Slider(min=0.01, max=1.0, defaultValue=0.5, step=0.01)
		Organ Minimum Radius: Slider(min=1, max=10000, maxSlider=200, defaultValue=1, step=1)
	Liver: Horizontal Separator:
		Detection Enabled: Switch(defaultValue=true)
		Size Multiplier: Slider(min=0.2, max=1.0, defaultValue=1.0, step=0.1)
		Confidence Threshold: Slider(min=0.01, max=1.0, defaultValue=0.5, step=0.01)
		Organ Minimum Radius: Slider(min=1, max=10000, maxSlider=200, defaultValue=1, step=1)
	Abdomen: Horizontal Separator:
		Detection Enabled: Switch(defaultValue=true)
		Size Multiplier: Slider(min=0.2, max=1.0, defaultValue=1.0, step=0.1)
		Confidence Threshold: Slider(min=0.01, max=1.0, defaultValue=0.5, step=0.01)
		Organ Minimum Radius: Slider(min=1, max=10000, maxSlider=200, defaultValue=1, step=1)
If user disables detection of an organ the components in the section will be disabled as well


On the "Mission Rules" there are following components:
	Rules: HorizontalSeparator
		Mission Type: TextField - this value is edited on the Modal Window
		Disable Duty Cycle: TextField - this value is edited on the Modal Window
		Delay Between Engagements: TextField - this value is edited on the Modal Window
		Trigger - Hold Duration: TextField - this value is edited on the Modal Window

Modal window of the "Mission Rules":
	Rules: HorizontalSeparator
		Mission Type: ComboBox(value="Random Walk", enabled=false)
		Disable Duty Cycle: Switch(value=on)
		Delay Between Engagements (s): Slider(min=0.01, max=5, value=0.5, step=0.05) 
		Trigger - Hold Duration: Slider(min=0.01, max=5, value=0.5, step=0.05)
	TriggerEvent - Motors Setup
	TriggerEvent - Execute OS Script

On the "Exit Strategy" panel there are following components:
	When to Execute Exit Strategy (logical OR): HorizontalSeparator
		Maximum Number of Engagements: TextField - this value is edited on the Modal Window
		Time After First AI Engagement: TextField - this value is edited on the Modal Window
		Predefined DateTime: TextField: TextField - this value is edited on the Modal Window

Modal window of the "Exit Strategy":
	When to Execute Exit Strategy (logical OR): HorizontalSeparator
		Maximum Number of Engagements: NumericInput(min=1, max=1000000000, value=1000000000)
		Time After First AI Engagement (s): NumericInput(min=1, max=1000000000, value=10000000000)
		Predefined DateTime: TextField: DateTime(minValue=now, value=2199-12-31:23:59:59)

Do not implement any backend logic. Implement only web page.

Update AI Setup web page Modal window.
- In the section "Engagement Essentials" change all Sliders to Numeric Edits. 
- In the "AI Disable Period" section change following properties
of the Table. Table column should be resizable.
- There should be new "Note" column added into the Table.
- Table at the moment does not display captions in the columns header. Fix it.
- When I click on the "Add Row" item in the Table s popup menu nothing happens.
There are no rows displayed before I click on the Add button and after I click on the Add button.
- Sliders width in the section "Organs / Body Parts" is too big. The Organ Minimum Radius Slider
is touching the text and on the other side it is touching the NumericEdit component.
That should not happen, there should be some gap.

Update AISetup web page.
Update AISetupMissionEditModal window. There will be an "On Engagement Event" Horizontal Separator at the end of the modal window.
Under it there will be a "Set Motor Position" Horizontal Separator (Section). This section will repeat 
for each motor from the settings.json -> aiSetup -> missions -> randomWalk -> motors. In the section of each motor there
will be very similar set of components like on the "Manual Control" web page modal window:
	Name: TextField containing name of the motor with a color in front of it
	Enabled: Switch
	Speed: Slider(min=-1, max=1, value=0, step=0.01, decimalPlaces=3)
Create restapiaisetup.py file which will handle the communication with the front end. There will be a function to 
send "aiSetup" json to the front end and as well a function to receive it from the front end and store it into the settings.json.
Update front end to persist users setup (when he clicks on the "Save" button on each modal window).

Update AISetup web page.
On the mission modal window change "Delay Between Engagements (s)" from Slider to NumericInput.
On the mission modal window change "Engagement Duration (s)" from Slider to NumericInput.
On the exit strategy modal window add "Exit Strategy Duration (s)": NumericInput(min=0.01, max=1000000000)
Add the "Exit Strategy Duration" as well to the "Exit Strategy" Panel. It will be displayed as a TextField
On the exit strategy modal window add to the end "On Execute Event" Horizontal separator.
Under it there will be a "Set Motor Position" Horizontal Separator (Section). This section will repeat 
for each motor from the settings.json -> aiSetup -> missions -> randomWalk -> motors. In the section of each motor there
will be very similar set of components like on the "Manual Control" web page modal window:
	Name: TextField containing name of the motor with a color in front of it
	Enabled: Switch
	Speed: Slider(min=-1, max=1, value=0, step=0.01, decimalPlaces=3)

Update AISetup web page.
1. Add a new section above the AI Disable Period called "Activation". Into this section add following components:
Activation Date Time: DateTime
2. Add this new section to the "AI Essentials" panel.
3. Remove AI Disable Period section including the Table from the Modal window.
Remove "aiDisablePeriods" from the json and from the rest api. Remove "AI Validity Period" from the "AI Essentials" panel.

Create a new "Code" component which will be a multiline text filed. It will be used to display
plain text or python code or json code. Use proportional font in the component. The component will have following props:
- fontSize: size of the font. By default it is the same as a regular text on the page.
- syntax: oneOf("text" - which means plain text, "python" - which means python code, the python syntax should be highlighted,
"json" - the json syntax should be highlhgted). Default value "text".
- showLineNumbers: boolean (default=true) - if set to true then there will be a line numbers displayed in the Code view.
- wordWrap: boolean (default=false) - if set to true then there will be no horizontal scrollbar and words will be wrapped.
Demonstrate usage of the component in the Sandbox. Create new web page and put the link to it
 into the menu under "Sandbox".

 Update AISetup web page, all of its modal windows, restapiaisetup and settings.json.
 All Datetimes "Activation Date and Time" on "AI Essentials" panel and modal window and
 "Fixed Date and Time" on Exit Strategy panel and modal window must not be displayed in UTC.
 They must be displayed in server time (not browser time), but they will be stored in UTC in the settings.json.

Update the General Setup web page. There will be a new panel inserted on the left side of the Controller Setup panel.
The panel name is "System". There will be following components:
Date and Time: HorizontalSeparator
	Date and Time: TextField with date and time in format 2025-12-31 23:59:59. The date and time is returned
	by the restapi and it is displayed as is - not in UTC, not in local web browser time zone, but as server returned it.
	Server always returns date and time in its time zone.
	Time Zone: TextField - timezone of the server.

There will be an Edit button in the right corner of the panel. When user clicks on the button the modal window will be displayed.
On the modal window the user can edit "Date and Time" of the server and "Time Zone" of the server. When user clicks on
the "Save" button
the backend is called and rest api sets the system DateTime and Time Zone. Create a new file called osmanagement.py where
you will put this datetime management functionality. Make sure that setup of system Date, Time and Time Zone works
seamlesly on Windows, Linux and Mac OS.

Update the DateTimePicker component. When the web browser window is too small and user clicks on the datetime picker
the popup menu opens but some parts of the popup menu overflows down below the accessible area of the browser.
If such situation happens the scrollbars should be displayed in the popup menu to allow user to access
all parts of the menu.

Update Cameras web page. On the left side of the Edit button on each panel add a "Reset all" button with 
the icon reload.svg. When user clicks on the button the following message is displayed:
Are you sure you want to reset all cameras? If user clicks on the "Yes" button
the restapi api/cameras/resetall is called. The endpoint resetall calls
master_controller.cameras_controller.reset() function and returns {"success": true}. After
the success is received in the front end, the values of all panels are refreshed, most probably
the api/cameras/list and api/cameras/input_devices is called.

Update Cameras web page. When user clicks on the "Yes" button - do not close the Modal window. Wait for
the success message. The buttons on the modal window should be disabled and instead of
the text "Yes" there should be a text "Reseting ..."

Update Cameras web page. Extract edit modal window and all of its functionallity and save it into CamerasEditModal.jsx

Update Cameras web page modal window. The sliders properties min value, max value, minSlider value and maxSlider value
in section "Image Settings" and "Controls"  should be set up
according to the api/cameras/input_devices[CAMERA_INDEX] -> capabilities. The enabled property should be as well set up according
to the capabilities settings. Decimal places of the NumericInputs of all sliders should be set to 2.
Since every input_device can have different setup of "capabilities" the slider props needs to be updated
every time when user selects an "Input Device".

Update Cameras web page and restapicameras.py and other files if necessary. We need to have 3 spotter cameras not only 1 like it is now.
On the Cameras web page add two panels
named "Spotter Camera 2" and "Spotter Camera 3". Rename "Spotter Camera" panel to "Spotter Camera 1".
Update rest api to save and load Spotter Camera 2 and Spotter Camera 3 into the settings.json.

Update Manual Control web page and other files if necessary. Now there are 3 spotter cameras. Therefore on the modal window
there should be in the cameras section three spotter cameras. Rename "Spotter Camera" MultiSwitch to "Spotter Camera 1".
Add "Spotter Camera 2" and "Spotter Camera 3" MultiSwitch. Change "Camera" MultiSwitch which currently
has only "Scope" and "Spotter Camera" values to ComboBox with "Scope", "Spotter Camera 1", "Spotter Camera 2" And
"Spotter Camera 3" options.
Update backend API to be able to save/load all three spotter cameras settings.

1. Update Hot Zone edit Modal window. Right now in the section "Arms" there is Left Arm Min Length, Left Arm Max Length,
Right Arm Min Length and Right Arm Max Length. Values are for example like this:
Left Arm Min Length: 60
Left Arm Max Length: 105
Right Arm Min Length: 60
Right Arm Max Length: 105
This must be changed. Instead of Left and Right Max Lengths there should be
a "Left Arm Stroke Len." and "Right Arm Stroke Len.". Min values of Stroke Lengths are 1, Max values are 100.
Stroke Length will be added on top of Min length so the example above will be rewritten using the new
setup values like this
Left Arm Min Length: 60
Left Arm Stroke Len.: 45
Right Arm Min Length: 60
Right Arm Stroke Len.: 45
2. Above the "Left Arm Min Length" Slider add following component:
Symetric Arms: Switch. If Symetric Arms are enabled then disable "Right Arm Min Length" and "Right Arm Stroke Len."
and whenever user updates Left Arm values (Left Arm Min Length, Left Arm Stroke Len.)
those should be written as well to the Right Arms values. Do not forget to update backend to save/load values.

There is something wrong with the Stroke len. Whenever I change left arm or right arm Stroke len nothing happens.
It was expected that the point cloud
should be recomputed taking into account that stroke length. In the past the three sphere intersection
was calculated based on the:
- min radius of left arm, max radius of left arm
- min radius of right arm, max radius of right arm 
- constant radius of mic stick.
Now it should be calculated as intersection of three spheres with:
- min radius of left arm, min radius of left arm + left arm stroke len.
- min radius of right arm, min radius of right arm + right arm stroke len. 
- constant radius of mic stick.
For some unknown reason this is not happening.
Fix it.

Update main Menu. Create new item called "Tools" (use "tools.svg" as an icon) and move "Hot Zone" web page
under the "Tools" item in the menu.

Update NumericInput component which is used on many pages and as well in the Slider component.
Currently when user clicks on the arrow button in the NumericInput component (and after click he holds the mouse button down)
the value changes only once and it does not matter how long the user holds the mouse button down.
New behavior should be like this: when user clicks on the arrow button (up or down)in the NumericInput and holds the mouse button down
the value changes, than nothing happens for at least a second and than the value increases (if user clicked on
the up button) or decreases (if user clicked on the down button) approximately every 0.1 second until user releases the mouse button
or until the min or max value is reached.

Rewrite motorscontroller.py MotorsController class that it will be a subclass of Threading.Thread. Rewrite, replace or delete
start, stop and _run methods.

Update page GeneralSetup. On the System panel before the "Date and Time" section add new section "System Info".
Create a backend api which returns result from common.py -> get_system_info() to the front end.
Into the "System Info" add following read only text components which will display data from
get_system_info():
Operating System: human_readable_os
Hardware: human_readable_hardware
User: human_readable_user
The "System Info" section is not displayed on the modal window. It is displayed only on the panel.

Update Motors web page. Update "Test Motor" Modal window. There should be timmer added on the modal window.
When the modal window is opened the Timer should be enabled. It should call "speed" api backed with the current position
of Joystick every 0.2 second. The timer should be disabled when the modal window is closed.

aiagent.py file is a little bit messy. What should we do to make it more readable and more performant?

In aiagent.py the function _compute_next_status does not work as expected. The behavior should follow state diagram:
- First the system is not_engaging.
- If result.immediate_engagement_condition_satisfied is True then the system start to arm.
  During arming phase the result.immediate_engagement_condition_satisfied must still be True
  otherwise the system goes back to not_engaging state.
  Arming phase duration is equal to settings.json -> aiSetup -> organMustBeVisibleSeconds.
- If result.immediate_engagement_condition_satisfied is still True and Arming phase duration elapsed
  the system changes status to ENGAGING which will last for at least 
  settings.json -> aiSetup -> mission -> randomWalk -> engagementDuration.
  As soon as ENGAGING state is set it does not matter that result.immediate_engagement_condition_satisfied is False.
  As soon as ENGAGING state is set a new record is added into _engagement_history.
- After the Engaging phase there is Disengaging phase which lasts for at least
  settings.json -> aiSetup -> mission -> randomWalk -> delayBetweenEngagements.
- After Disengaging phase the system goes to not_engaging state.
- In case settings.json -> aiSetup -> exitStrategy -> maxEngagements is reached meaning there are
  at least maxEngagements records in the _engagement_history list or
  in case settings.json -> aiSetup -> exitStrategy -> timeoutAfterFirstEngagement seconds
  elapsed since first engagement or
  in case settings.json -> aiSetup -> exitStrategy -> 2199-12-31T22:59:59.000Z happened
  the system is set to EXIT_STRATEGY_UNDER_EXECUTION state
- There is as well WAITING_TO_START state which prevents state above to be set
- There is as well FPS_CONDITION_NOT_SATISFIED state which prevents ARMING and ENGAGING
  states to be set.  

Update aiagent.py. AIAgent class should be a subclass of the Thread. The run method is started
immediatelly after the instance of the class is created. There is an infinite loop in the run
method which waits one second, then it randomly picks the "length of the movement" from 1 to 4 seconds.
It randomly picks 2D Vector, where x means speed x, and y means speed y, and moves a leftArm and rightArm motor
for a "length of the movement" in direction x and y. Then the loop repeats but in the second
run and every other run the system randomly decides with 50% probability if it will move in the same direction
as during previous run or if it will choose a different one. New "length of the movement" is selected
in every run. The thread can be stopped and paused at any moment and the thread will be stopped
when the application is closed.

There is a problem on the manualcontrol web page. If the user selects a "slider" mode of the motor on the Modal window
instead of "joystick" mode and if he then slides the the joystick comopnent for example to value
1 and releases mouse button. The button of the joystick component stays at 1, but the timer calls "action" rest api
it calls it with value 0 instead of 1. Fix the issue.

There is a problem on manual control web page. Whenever the web page is loaded or when user switches among cameras
(scope camera, spotter camera 1, spotter camera 2, spotter camera 3) the reticle color and reticle outline color
stays unchanged. They should change according to the settup in the settings.json of selected camera.

Update the application onload function. When the onload is called the system should load
"settings.json -> general -> startupScript -> operating_system_code" where operating_system_code
is returned from common.py -> get_system_info. Warning 
"settings.json -> general -> startupScript -> operating_system_code" may not exist. You should test for existence of this parameter.
The system then executes the script. It does not matter if the script fails. Errors should be printed out, but
application should continue to work.

Add new function wifi_startup into main.py. The wifi_startup will be called from onload function. wifi_startup
will return immediately if the operating_system_code does not start with "raspberrypi". If it starts with
raspberrypi then the system takes a look into "settings.json -> general -> wifi -> mode". If the mode is "accessPoint"
then the system loads "settings.json -> general -> wifi -> accessPoint" parameters and executes following
shell commands:
1. sudo nmcli radio wifi off 
2. sudo nmcli radio wifi on
3. sudo nmcli device wifi hotspot ifname wlan0 ssid "{ssid}" password "{password}"
where it fills in "ssid" and "password" from settings.json
If the mode is "client" then the system loads "settings.json -> general -> wifi -> client" parameters and executes
following shell commands:
1. sudo nmcli radio wifi off 
2. sudo nmcli radio wifi on
# in case security is None, or null, or "none"
3. sudo nmcli connection add type wifi ifname wlan0 con-name "{connectionName}" ssid "{ssid}"
# in case security is "wep"
3. sudo nmcli connection add type wifi ifname wlan0 con-name "{connectionName}" ssid "{ssid}" -- wifi-sec.key-mgmt none wifi-sec.wep-key0 "{password}" wifi-sec.auth-alg open
# in case security is wpa-psk
3. sudo nmcli connection add type wifi ifname wlan0 con-name "{connectionName}" ssid "{ssid}" -- wifi-sec.key-mgmt wpa-psk wifi-sec.psk "{password}"
# in case security is wpa3
sudo nmcli connection add type wifi ifname wlan0 con-name "{connectionName}" ssid "{ssid}" -- wifi-sec.key-mgmt sae wifi-sec.psk "{password}"
If the mode is other than accessPoint or client then the function will return.
This function may fail in that case print the error, but application must continue to work.

Add new panel between System and Controller Setup on the general setup web page. The panle name will be "Wifi". There will be following
components on the panel:
Mode: TextField
Access Point: HorizontalSeparator
	connectionName: TextField
	ssid: TextField
	security: TextField
	password: TextField
Client: HorizontalSeparator
	connectionName: TextField
	ssid: TextField
	security: TextField
	password: TextField
Values of the modal window are loaded from the "settings.json -> general -> wifi"
There is an edit button in the right corner of the panel. When user clicks on the edit button the Modal window with following components appears:
Mode: ComboBox(values=["disabled", "accessPoint", "client"], default="accessPoint")
Access Point: HorizontalSeparator
	connectionName: TextBox(max_letters=32)
	ssid: TextBox(max_letters=32)
	security: ComboBox(values["wpa-psk"], disabled=True)
	password: TextField(max_letters=32)
Client: HorizontalSeparator
	connectionName: TextBox(max_letters=32)
	ssid: TextBox(max_letters=32)
	security: ComboBox(values["none", "wep", "wpa-psk", "wpa3"], default="wpa-psk")
	password: TextField(max_letters=32)
If the mode is "disabled" then entire section "Access Point" and "Client" are disabled.
If the mode is "accessPoint" then the section "Client" is disabled.
If the mode is "client" then the section "Access Point" is disabled.
If user selects security=none in client section then the password textbox will be disabled, otherwise it is enabled.
There is a save and cancel button on the modal window. If user clicks on the save button the setup is saved to settings.json.

Add new section into the System panel in the general setup web page. The section name will be "Reboot". There will be a red button
with name "Reboot" in the section. When user clicks on the red button the warning message will appear asking user:
Do you really want to reboot the system (Raspberry Pi)?
There will be two buttons Cancel and OK on the bottom of the message. If user clicks on OK button the system will be rebooted.

Add a new section before "Duty Cycle" section on the modal window of the motors web page. Do not add this section to the panel.
It should remain only on the Modal window. There will be following components in the section:
Advanced Options: HorizontalSeparator
	Display Advanced Options: Switch(default value=False)
If user enables "Display Advanced Options" the sections "Duty Cycle", "Histogram Quick Test" and "Speed Histogram"
will be visible, otherwise they will be hidden.

Create a svg image of raspberry pi 5 zoomed in on J8 pin.
The orientation of the pins should be vertical. There should be a description of each pin.
Description of 3.3V and 5V pins should have red background and black font.
Description of ground pins should have black background and white font.
Description of other pins should have light grey background and black font.
There should be an arrow indicating where are USB ports and Ethernet port.
There should be an arrow indicating where is the sd card port.
Save the immage into the assets directory as j8schema.svg.

Update AI Setup. Into the panel and into the Modal window into the section "Engagement Essentials" add
a component:
AI Model:ComboBox(values=YOLOModels.models.keys)
Update backend API to load and save model value into settings.json -> aiSetup -> modelName

Update motors web page. Add a button "Display Pin Diagram" below "PWM Frequency (Hz)" on the Modal window.
When user clicks on the button an assets/j8schema.svg is opened in another tab in the browser.

Update general setup web page. On the Modal Window add a business validation for the section "Access Point".
Password length should be at least 8 letters long. If the password is not 8 letters long the Modal window
cannot be saved and the error button which displays error message will appear. Take a look on the motors web
page to see how the error and warning messages are implemented.

Modify following components:
Button
CheckBox
ColorPicker
ComboBox
DateTimePicker
FileInput
MultiSwitch
NumericInput
Slider
StaticText
Switch
Textbox
TextField
Add a new props to those components called "outline". Outline will be a color. Default value is null.
If outline is specified then the component will be outlined in a similar way how it is done on the Motors page
when user selects the same pin on the modal window. If outline is not specified - then the component
will be displayed like it is right now. Demonstrate the outline in the Sandbox -> ComponentsDemo web
page.

When user selects a model on the AI Setup web page (for example pose26x) which is the last item in the ComboBox and user
saves the selection then the first model in the YOLOModels which is pose26n is used. Why is it happening?
Update the system behavior.

Update restcamera.py. Whenever user opens a modal window on the Cameras web page, lets say he is updating scope_camera, and 
selects different camera device with different index and presses Apply button or Save button
the camera_code of the old camera should be set to None and camera_code of the newly selected camera should be set
to for example scope_camera.

On the AI Setup web page in the panel and on the Edit modal window add "Device": Combobox with following options:
"cpu (optimized for x86)"
"arm_cpu (optimized for ARM)"
"cuda:0 (Nvidia GPU)"
Default value is "cpu (optimized for x86)"
The "Device" combobox should be placed under the "Model" combobox. The value should be stored and loaded
from settings.json -> aiSetup -> device

Update cameras modal window. Whenever user modifies value on one of the following components on the Modal window
1. the "Apply" button and the "Save" button should be outlined using orange color
2. the camera.stop function should be called to stop live stream on the polygon component
3. the icon that the stream is not available should be displayed on the live stream polygon.
This functionallity is already partially implemented for a few components, but you should probably reimplement it
from scratch. This behavior should be used for all components in all sections except:
- all components place in section "Static Reticle"
- all components in section "Preview" except of "HTTP Stream Quality" and "Display Mode"

Jsi expert na marketing a PR. Napište text, který je stručný, ale osloví všechny potencionální klienty, aby si chtěli software stáhnout a vyzkoušet. Text piš v angličtině. Text bude umístěn na hlavní stránce webového portálu. V textu popiš, že na této stránce si může uživatel stáhnout aplikaci sloužící k extrémně přesné manipulaci s těžkými předměty typicky s těžkými kamerovými systémy nebo s teleskopickým mikrofonem. Manipulaci je možné provádět manuálně, ale také je možné tuto činnost přenechat AI. Přesnost pohybu je sub moa. Software obsahuje nejmodernější AI pro detekci různých předmětů typicky lidí ať již stojících, nebo lidí v pohybu. Na tomto poli nemá software konkurenci, neboť dokáže detekovat jednotlivé části lidského těla, fokusovat se na ně, spouštět různé externí akce požadované uživatelem v rámci nastavení mise a po dokončení mise spustí návazné akce tzv. exit strategy.
Kromě turret systémů obsahuje také podporu turretless systémů založených na lineárních aktuátorech, které manipulují s těžkými předměty s vyoseným těžištěm. Tzv. self compressing free floating setup.
Software je volně dostupný a vyvíjený, jako open source. Tým jej neustále vyvíjí a zdokonaluje.
Výsledek ulož do souboru readme.md.

Postupuj dále. Pod již existujícím textem popiš kroky na zprovoznění:
1. Kliknout na menu a pak na Hot Zone. Zde nakonfigurovat turretless systém.
2. Koupit potřebný materiál.
- Raspberry Pi 5 (2GB) starter kit, nebo novější.
- Kamera (doporučená Pi Camera 3)
- Motor Driver BTS7960 (někdy též nazývaný HW-039, nebo IBT_2).
- Dupont Wires 40x20cm.
- Koupit lineární aktuátor 12V, 1000+ N, stroke size podle potřeby typicky 20cm (8 palců) až 45cm (18 palců), čím pomalejší, tím přesnější. Doporučovaná rychlost je 5mm - 10mm za sekundu.
- Kevlarový vlasec, který udrží hmotnost alespoň 80 Kg.
Volitelně koupit:
- teflonovou trubičku s vnitřním průměrem 1,75 nebo 2 mm a vnějším průměrem 3, nebo 4 mm.
- Zdroj 12V 10A, nebo baterii, nebo autobaterii.
- Kabely ke zdroji - dvoulinka s měděným lankem/drátem o průměru v průřezu alespoň 1mm
- Elektrická svorkovnice
- Šroubovák
- Zip Ties
- Duct Tape
3. Nastavit motory v Menu -> Setup -> Motors
4. Nastavit kamery v Menu -> Setup -> Cameras
5. Nastavit AI

Ve složce /server/new_components/src/components jsou uloženy novější verze komponent
než které se používají v /server/wwwroot/src/components. Identifikuj všechny změny,
které bude nutné udělat pro přenos nových komponent do složky
/server/wwwroot/src/components
identifikuj všechny soubory, ve kterých bude nutné něco upravit, aby korektně
pracovaly s novými komponentami.
Pak komponenty přenes
a změny aplikuj.

Proveď hloubkové review všech komponent a stránek ve složce /server/wwwroot.
Zjisti co vše by se dalo vylepšit pro zlepšení spolehlivosti a rychlosti projektu,
případně pro zvýšení čitelnosti zdrojového kódu. Pozor spolehlivost,
multiplatformnost a rychlost kódu je prioritizována před čitelností.
Soustřeď se na to, aby kritické části byly thread-safe a fault-proof.
Je plánováno, že tento systém poběží bezodstávkově měsíce, možná roky.

Načti findings_extended.md, což je soubor, který byl vytvořen jako detailní review zdrojových kódů tohoto projektu.
Zaměř se pouze na aktivní zjištění 1 "server/wwwroot/src/lib/api.js — hardcoded http://hostname:80" a oprav tuto závadu jako expert.
Uvědom si, že tento systém bude procházet auditem, proto musí být tvoje oprava super seniorní.
Pokud máš doplňující dotazy, neboj se mě zeptat.

Načti findings_extended.md, což je soubor, který byl vytvořen jako detailní review zdrojových kódů tohoto projektu.
Zaměř se pouze na aktivní zjištění 2 `server/wwwroot/src/App.jsx` — `BrowserRouter` bez `basename` a oprav tuto závadu jako expert.
Uvědom si, že tento systém bude procházet auditem, proto musí být tvoje oprava super seniorní.
Pokud máš doplňující dotazy, neboj se mě zeptat.

Načti findings_extended.md, což je soubor, který byl vytvořen jako detailní review zdrojových kódů tohoto projektu.
Zaměř se pouze na aktivní zjištění 3 `server/wwwroot/src/components/Timer.jsx` — stale callback při aktivním intervalu a oprav tuto závadu jako expert.
Uvědom si, že tento systém bude procházet auditem, proto musí být tvoje oprava super seniorní.
Tvoje oprava tedy znamená, že pokud se pošle request na server, tak stránka počká na odpověď. Pokud odpověď trvala
déle než je timer.interval tak zašle další request okamžitě, v opačném případě počká, aby uběhl čas
timer.interval a potom pošle další request? V nejhorším případě počká
network timeout a pak pošle request znovu?

Načti findings_extended.md, což je soubor, který byl vytvořen jako detailní review zdrojových kódů tohoto projektu.
Zaměř se pouze na aktivní zjištění 4 `server/wwwroot/src/ManualControl.jsx` — příliš agresivní control loop a oprav tuto závadu jako expert.
Uvědom si, že tento systém bude procházet auditem, proto musí být tvoje oprava super seniorní.
Ověř, jestli je toto zjištění ještě platné.
Pokud máš doplňující dotazy, neboj se mě zeptat.

Načti findings_extended.md, což je soubor, který byl vytvořen jako detailní review zdrojových kódů tohoto projektu.
Zaměř se pouze na aktivní zjištění 5 `server/wwwroot/src/ManualControl.jsx` — async načítání bez plné cancellation strategie a oprav tuto závadu jako expert.
Uvědom si, že tento systém bude procházet auditem, proto musí být tvoje oprava super seniorní.
Pokud máš doplňující dotazy, neboj se mě zeptat.

Načti findings_extended.md, což je soubor, který byl vytvořen jako detailní review zdrojových kódů tohoto projektu.
Zaměř se pouze na aktivní zjištění "## 6." a oprav tuto závadu jako expert.
Uvědom si, že tento systém bude procházet auditem, proto musí být tvoje oprava super seniorní.
Pokud máš doplňující dotazy, neboj se mě zeptat.

Načti findings_extended.md, což je soubor, který byl vytvořen jako detailní review zdrojových kódů tohoto projektu.
Zaměř se pouze na aktivní zjištění "## 7." a oprav tuto závadu jako expert.
Uvědom si, že tento systém bude procházet auditem, proto musí být tvoje oprava super seniorní.
Pokud máš doplňující dotazy, neboj se mě zeptat.

Načti findings_extended.md, což je soubor, který byl vytvořen jako detailní review zdrojových kódů tohoto projektu.
Zaměř se pouze na aktivní zjištění "## 8." a oprav tuto závadu jako expert.
Uvědom si, že tento systém bude procházet auditem, proto musí být tvoje oprava super seniorní.
Pokud máš doplňující dotazy, neboj se mě zeptat.

Načti findings_extended.md, což je soubor, který byl vytvořen jako detailní review zdrojových kódů tohoto projektu.
Zaměř se pouze na aktivní zjištění "## 9." a oprav tuto závadu jako expert.
Uvědom si, že tento systém bude procházet auditem, proto musí být tvoje oprava super seniorní.
Pokud máš doplňující dotazy, neboj se mě zeptat.

Načti findings_extended.md, což je soubor, který byl vytvořen jako detailní review zdrojových kódů tohoto projektu.
Zaměř se pouze na aktivní zjištění "## 10." a oprav tuto závadu jako expert.
Uvědom si, že tento systém bude procházet auditem, proto musí být tvoje oprava super seniorní.
Pokud máš doplňující dotazy, neboj se mě zeptat.

Načti findings_extended.md, což je soubor, který byl vytvořen jako detailní review zdrojových kódů tohoto projektu.
Zaměř se pouze na aktivní zjištění "## 11." a oprav tuto závadu jako expert.
Uvědom si, že tento systém bude procházet auditem, proto musí být tvoje oprava super seniorní.
Pokud máš doplňující dotazy, neboj se mě zeptat.
